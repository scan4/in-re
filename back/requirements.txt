# FastAPI Web 框架
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# 异步 HTTP 客户端 (调用外部 API)
httpx>=0.27.0

# PostgreSQL 异步驱动 + ORM
asyncpg>=0.30.0
sqlalchemy[asyncio]>=2.0.30

# PostgreSQL 同步驱动 (pgvector 向量操作)
psycopg2-binary>=2.9.0
pgvector>=0.2.5

# Redis 异步客户端 (LLM 评分缓存)
redis>=5.0.0

# BGE Embedding 本地模型
sentence-transformers>=3.0.0

# 数据校验
pydantic>=2.0
pydantic-settings>=2.0

# JWT 认证
python-jose[cryptography]>=3.3.0

# 定时任务
apscheduler>=3.10.0

# 环境变量
python-dotenv>=1.0.0

# 日志
structlog>=24.0.0
