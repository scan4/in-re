# 智能推荐 Agent 代码逻辑文档

> 版本: V5 | 日期: 2026-07-29 | 架构: FastAPI + SQLAlchemy async + asyncpg/psycopg2 → PostgreSQL + pgvector + BGE 向量 + LLM

---

## 目录

1. [整体架构](#1-整体架构)
2. [推荐全链路流程](#2-推荐全链路流程)
3. [各模块详解](#3-各模块详解)
   - 3.1 main.py, 3.2 config.py, 3.3 api/recommend.py (JWT鉴权+权限同步), 3.4 orchestrator_v2.py
   - 3.5 data_repo.py (权限过滤), 3.6 llm_scorer.py, 3.7 embedding.py, 3.8 circuit_breaker.py
   - 3.9 connection.py, 3.10 schema_v3.py, 3.11 migrate_v3.py, 3.11b populate_vectors.py
   - 3.12 scheduler.py, 3.13 content_sync_v2.py, 3.14 behavior_sync_v2.py, 3.15 external_api.py
   - 3.16 schemas/recommend.py, 3.17 llm_config.py (厂商配置), 3.18 redis_client.py, 3.19 权限隔离表
4. [数据库表结构](#4-数据库表结构)
5. [定时同步机制](#5-定时同步机制)
6. [冷启动策略](#6-冷启动策略)
7. [降级与容错](#7-降级与容错)
8. [踩坑记录与经验总结](#8-踩坑记录与经验总结)

---

## 1. 整体架构

```
┌─────────────────────────────────────────────────────┐
│                     前端 (Vite + React)              │
│               用户点击 6 个模拟用户按钮                │
│          POST /api/v1/recommend 发送推荐请求          │
└──────────────────────┬──────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────┐
│                   FastAPI 服务层 (main.py)            │
│  ┌─────────────────┐  ┌──────────────────────────┐  │
│  │ api/recommend.py │  │  sync/scheduler.py       │  │
│  │ /recommend       │  │  定时同步 (APScheduler)   │  │
│  │ /recommend/feed  │  │  ├─ 内容: 每30分钟        │  │
│  └────────┬─────────┘  │  └─ 行为: 每5分钟         │  │
│           │            └──────────────────────────┘  │
├───────────┼──────────────────────────────────────────┤
│           ▼                                          │
│  ┌───────────────────────────────────────────────┐  │
│  │         推荐引擎 (engine/orchestrator_v2.py)    │  │
│  │                                               │  │
│  │  ① user_id 查画像 + 行为     (data_repo.py)   │  │
│  │  ② BGE 语义初筛              (embedding.py)   │  │
│  │  ③ LLM 评分 + 理由           (llm_scorer.py)  │  │
│  │  ④ 本地融合计算               (orchestrator)   │  │
│  │  ⑤ 冷启动检测 + 时间衰减                       │  │
│  └───────────────────────────────────────────────┘  │
└──────────────────────┬──────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────┐
│              PostgreSQL 数据库                       │
│  ┌──────────┬──────────┬───────────┬──────────────┐ │
│  │ courses  │ training │ skill_    │ assessment_  │ │
│  │          │ _classes │ positions │ scales       │ │
│  ├──────────┼──────────┼───────────┼──────────────┤ │
│  │ career_  │ news     │ user_     │ field_       │ │
│  │ plans    │          │ behaviors │ importance   │ │
│  ├──────────┴──────────┴───────────┴──────────────┤ │
│  │ user_profile  │ recommend_log  │  sync_log     │ │
│  └────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────┘
```

**关键技术栈**: FastAPI (异步 Web 框架) → SQLAlchemy[async] (ORM) → asyncpg (数据库驱动) → PostgreSQL

**新增组件**:
- **用户权限隔离** (`user_content_visible` 表): 用户首次访问时用其 JWT Token 拉取内容接口，记录可访问的 origin_id。推荐时 `get_all_candidates` 通过 `INNER JOIN` 该表过滤，实现不同用户看到不同内容池。
- **LLM 配置侧边栏** (`api/llm_config.py`): 前端侧边栏允许切换厂商 (DeepSeek/通义千问/Kimi/智谱)，填写 API Key 和模型名，配置保存到 `llm_config.json`，即时生效。
- **JWT Token 鉴权** (`api/recommend.py`): 前端 URL 传入 `?tokens=` 参数，后端解析 JWT payload 提取 `phone` 字段作为 `user_id`，新用户自动注册到 `user_profile` 并补全画像。
- **Redis LLM 缓存** (`db/redis_client.py`): LLM 评分结果以 `llm_score:{user}:{type}:{cand_fp}:{ctx_fp}` 为 Key 缓存 10 分钟，缓存命中时跳过 API 调用。

---

## 2. 推荐全链路流程

### 2.1 请求入口

```
POST /api/v1/recommend
{
  "user_id": "shty80002",       ← 精确匹配用户
  "context_type": "browsing",   ← 浏览/搜索
  "context_text": "李明",        ← 当前浏览内容文本
  "content_types": ["course", "training", "skill", "scale", "career", "news"],
  "limit": 4
}
```

### 2.2 完整流程

```
api/recommend.py::recommend()
  │
  └─ engine/orchestrator_v2.py::run_recommend_v2()
       │
       ├─ ① 查画像 + 行为（一次查询，复用给所有类型）
       │     get_user_profile(user_id)     → user_profile 表
       │     get_user_behaviors(user_id)   → user_behaviors 表
       │
       ├─ ② 冷启动检测
       │     if 无专业 AND 无技能 AND 无行为记录:
       │         → _cold_start_recommend() (按字段完整度排序，不调LLM)
       │
       ├─ ③ 6种 content_type 并行处理 (asyncio.gather)
       │     │
       │     └─ _recommend_for_type(profile, behaviors, context_text, ct, limit)
       │          │
       │          ├─ ③a. 拉全量候选
       │          │     get_all_candidates(content_type) → SELECT * FROM {table}
       │          │
       │          ├─ ③b. 加载字段重要性
       │          │     get_field_importance(table_name) → field_importance 表
       │          │
       │          ├─ ③c. BGE 语义初筛
       │          │     build_user_vector_text() → 画像字段 + 最近5条行为
       │          │     pre_screen_candidates()  → 编码用户+候选 → cos相似度
       │          │     过滤 < 0.45 → 保留 top-30 (+3条兜底)
       │          │
       │          ├─ ③d. LLM 评分 + 理由
       │          │     llm_score_and_rank()     → 30条候选 + 上下文 + 字段重要性
       │          │     LLM返回: {content_id: {score, reason}, ...}
       │          │
       │          └─ ③e. 本地融合计算
       │               公式:
       │               正常: LLM×0.7 + BGE×0.1 + 字段完整性×0.05 + 时间衰减×weight
       │               降级: BGE×0.5 + 字段完整性×0.35 + 时间衰减×0.15
       │               过滤: final_score < 0.4 → 丢弃
       │
       └─ ④ 全局排序 → 返回
```

---

## 3. 各模块详解

### 3.1 `main.py` — FastAPI 服务入口

| 文件路径 | `back/main.py` |
|---------|---------------|

#### 生命周期管理器

**`lifespan(_app)`** — FastAPI 异步上下文管理器

启动时:
1. 执行 `run_all_sync_now()` — 首次全量同步所有内容类型 + 用户行为
2. 调用 `start_scheduler()` — 注册 APScheduler 定时任务

关闭时:
2. 调用 `stop_scheduler()` — 关闭调度器

#### API 端点

| 端点 | 方法 | 函数 | 说明 |
|------|------|------|------|
| `/api/v1/health` | GET | `health_check()` | 健康检查，验证数据库连接 |
| `/api/v1/recommend` | POST | `recommend(req)` | 获取个性化推荐 |
| `/api/v1/recommend/feedback` | POST | `feedback(req)` | 上报推荐反馈（曝光/点击） |
| `/api/v1/sync/trigger` | GET | `trigger_sync()` | 手动触发全量同步 |

---

### 3.2 `config.py` — 环境配置

**文件路径**: `back/config.py`

从 `.env` 文件加载环境变量，提供以下配置常量：

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| `API_PORT` | 8000 | FastAPI 监听端口 |
| `DATABASE_URL` | `postgresql://.../recommend_agent` | PostgreSQL 连接串 |
| `LLM_PLATFORM` | `deepseek` | 大模型平台 |
| `LLM_MODEL` | `deepseek-v4-pro` | 模型名称 |
| `LLM_TIMEOUT_MS` | 60000 | LLM 请求超时 |
| `SYNC_INTERVAL_CONTENT` | 30 | 内容同步间隔（分钟） |
| `SYNC_INTERVAL_BEHAVIOR` | 5 | 行为同步间隔（分钟） |
| `LOG_LEVEL` | `INFO` | 日志级别 |

---

### 3.3 `api/recommend.py` — 推荐 API 路由

**文件路径**: `back/api/recommend.py`

**`_parse_jwt_payload(token)`** — 手动解析 JWT payload:
- `token.split('.')` → 取中间段 → base64 解码 → JSON → 提取 `phone`/`username`
- 不验签，网关层已验证

**`_ensure_user_exists(phone, username, token)`** — 首次访问自动注册:
1. 查 `user_profile` → 已有完整画像(major)则跳过
2. 从 JWT 创建最小画像 `{name: username}` → INSERT ON CONFLICT DO NOTHING
3. 调 `fetch_api_data("behavior")` → 从 signup 记录补全 major/college/education/skills
4. 仅 `REQUIRE_PERMISSIONS=true` 时调用 `_sync_user_permissions()` 建立可见内容权限

**`_sync_user_permissions(session, user_id, token)`** — 用户权限同步:
- 用该用户的 Token 并发调 5 个内容接口 (courses/training/skills/scales/career)
- 检查完整性：≥4/5 类型有数据才写入 `user_content_visible` 表
- 调试模式 (`REQUIRE_PERMISSIONS=false`) 跳过

**`recommend(req: RecommendRequest) -> RecommendResponse`**
- 接受 `token` (JWT)、`context_type`、`context_text`、`content_types`、`limit`
- 解析 JWT → 提取 user_id → 自动注册 → 委托给 `run_recommend_v2()`

**`feedback(req: FeedbackRequest) -> dict`** — 推荐曝光/点击记录写入 `recommend_log` 表

---

### 3.4 `engine/orchestrator_v2.py` — 推荐引擎编排器 (核心)

**文件路径**: `back/engine/orchestrator_v2.py`

#### 公开函数

**`run_recommend_v2(user_id, context_type, context_content_id, context_text, limit, content_types) -> dict`**

推荐入口，完整流程:
1. 查用户画像 + 行为记录（一次查询复用）
2. 冷启动检测：无专业/技能/行为 → 走冷启动流程
3. 6 种 content_type 并行处理 (`asyncio.gather`)
4. 全局按 score 降序排列返回

#### 内部函数

**`_cold_start_recommend(profile, behaviors, content_types, limit) -> dict`**

新用户冷启动策略:
- 每种类型拉全量候选 → 按字段完整性得分排序 → 取 top-N
- 不调 LLM（无用户上下文可匹配）
- 使用模板理由

**`_recommend_for_type(profile, behaviors, context_text, content_type, limit) -> dict`**

单个内容类型的推荐处理:
1. 拉全量候选 → `get_all_candidates(content_type)`
2. 加载字段重要性 → `get_field_importance(table)`
3. BGE 语义初筛 → `pre_screen_candidates()`
4. LLM 评分 + 理由 → `llm_score_and_rank()`
5. 本地融合计算 → 公式见下文

**`_calc_field_completeness(item, field_imp) -> float`**

计算字段完整性得分（归一化到 [0, 1]）:
- 每个字段按 `importance` × `weight` 加权
- 有内容的字段得分 = `imp × weight × (0.3 + 0.7 × 长度因子)`
- 归一化: 实际总分 / 完美情况下总分

**`_calc_time_decay(item, content_type, cfg, now) -> float`**

时间衰减计算:
- 资讯: 90天半衰期 `2^(-days/90)`
- 培训班: 60天半衰期 + 报名截止日后降权到 0.5
- 技能岗位: 180天半衰期
- 课程/量表/方案: 无衰减 (return 1.0)

**`_template_reason(title, content_type) -> str`**

LLM 不可用时生成模板推荐理由

**`_build_response(items, fallback_used) -> dict`**

格式化推荐响应，添加 trace_id

#### 融合打分公式

| 场景 | 公式 |
|------|------|
| **正常** | `LLM×0.7 + BGE×0.1 + 字段完整性×0.05 + 时间衰减×weight` |
| **降级** | `BGE×0.5 + 字段完整性×0.35 + 时间衰减×0.15` |
| **冷启动** | `字段完整性得分` (纯内容质量排序) |

其中 `weight` 取值:
- 资讯/培训班/岗位: 0.15
- 课程/量表/方案: 0

过滤: 最终分数 < 0.4 的结果直接丢弃。

---

### 3.5 `engine/data_repo.py` — 数据仓库 + pgvector 向量检索

**文件路径**: `back/engine/data_repo.py`
**注意**: `vector_search` 使用 psycopg2 同步连接（非 SQLAlchemy async），因为 asyncpg 的 prepared statement 不兼容 pgvector 的 `::vector` 类型转换。

#### 数据查询

**`get_user_profile(user_id: str) -> dict | None`** — 精确查画像

**`get_user_behaviors(user_id: str, limit=20) -> list[dict]`** — 查行为记录

**`get_all_candidates(content_type: str, user_id: str = "") -> list[dict]`** — 从分表拉候选，支持权限过滤:
- `user_id` 非空时检查 `user_content_visible` 表是否有该用户-该类型的权限记录
- 有权限 → `INNER JOIN user_content_visible` 过滤，仅返回该用户可见的内容
- 无权限 → 返回全量 (调试模式) 或 返回空 (生产模式，`REQUIRE_PERMISSIONS=true`)
- news 类型始终不走权限过滤（全平台共享资讯）

**`get_field_importance(table_name: str) -> dict`** — 查字段重要性

#### pgvector 向量检索（替代内存暴力计算）

**`vector_search(user_vector_text, content_type, top_k=30) -> dict[str, float]`**
- 用户文本 BGE 编码一次 → 查 `content_vectors` 表 → 用 pgvector 的 `<=>` 余弦距离算子排序
- 返回 `{content_id: similarity}` 字典
- 内容向量在同步时预计算存储，推荐时只编码用户向量

**`pre_screen_candidates(all_candidates, field_importance, max_keep=30, user_vector_text="") -> list[dict]`**
- 调用 `vector_search()` 获取 top-K 相似内容
- 命中向量检索的 + 少量未命中兜底（多样性）
- 给每条候选附加 `_sim` 字段，供后面本地融合打分使用

#### 用户文本构建

**`build_user_vector_text(profile, behaviors) -> str`** — 画像字段 + 最近5条行为

**`build_candidate_text(item, field_importance) -> str`** — 拼接 importance ≥ 4 的字段

**`build_user_context(profile, behaviors, context_text) -> str`** — 传给 LLM 的自然语言上下文

---

### 3.6 `engine/llm_scorer.py` — LLM 客户端

**文件路径**: `back/engine/llm_scorer.py`

#### 核心函数

**`llm_score_and_rank(user_context, candidates, field_importance, content_type, limit) -> dict[str, dict]`**

LLM 评分 + 理由生成:
1. 构建 prompt: 字段重要性说明 + 用户上下文 + 候选列表（只展示 importance≥3 的字段）
2. LLM 返回: `{"content_id": {"score": 0.95, "reason": "15字以内理由"}, ...}`
3. 多策略 JSON 解析（直接解析 → 提取 json codeblock → 提取花括号）

#### 辅助函数

**`chat(messages, temperature, max_tokens) -> str`** — 通用 Chat API 调用

**`_build_importance_info(field_importance) -> str`** — ★星级格式化字段重要性

**`_format_candidate(index, item, field_importance) -> str`** — 按重要性格式候选内容

**`_parse_json_response(content) -> dict`** — 多策略 JSON 解析

#### 异常类

**`LLMError`** — LLM 调用失败的异常

---

### 3.7 `engine/embedding.py` — BGE 向量编码

**文件路径**: `back/engine/embedding.py`

**`encode_text(text: str) -> list[float]`**
- 单条文本编码，返回 512 维向量

**`encode_batch(texts: list[str]) -> list[list[float]]`
- 批量编码，返回 512 维向量列表

**`cosine_similarity(vec1, vec2) -> float`**
- 余弦相似度，返回 [0, 1] 浮点数

**模型**: `BAAI/bge-small-zh-v1.5` (384维 → 但实际配置为512维)

---

### 3.8 `engine/circuit_breaker.py` — LLM 熔断器

**文件路径**: `back/engine/circuit_breaker.py`

#### 类

**`CircuitBreaker`**
- 配置: failure_threshold=3, recovery_timeout=30秒
- `state`: CLOSED/OPEN/HALF_OPEN
- `failure_count`: 连续失败计数
- `last_failure_time`: 最后失败时间

**`call(fn, *args, **kwargs)`** — 受保护的调用:
- CLOSED 状态直接调用，成功后 `reset()`，失败后 `record_failure()`
- OPEN 状态直接抛 `CircuitOpenError`，检查 `can_retry()` 尝试 HALF_OPEN

#### 异常类

**`CircuitOpenError`**

#### 全局实例

**`llm_breaker = CircuitBreaker(failure_threshold=3, recovery_timeout=30)`**

---

### 3.9 `db/connection.py` — 数据库连接

**文件路径**: `back/db/connection.py`

**`get_db()`** — FastAPI 依赖注入用，异步上下文管理器，自动关闭 session

**`get_db_session()`** — 直接返回 AsyncSession 实例，用于后台任务

**引擎配置**: pool_size=10, max_overflow=20, pool_pre_ping=True

---

### 3.10 `db/schema_v3.py` — 建表 SQL

**文件路径**: `back/db/schema_v3.py`

**`get_schema_sql() -> str`** — 返回完整 DDL

包含 7 张表: courses, training_classes, skill_positions, assessment_scales, career_plans, news, user_behaviors。每张表只保留 API 返回字段 + 3个运维字段 (id, status, sync_at)。

---

### 3.11 `db/migrate_v3.py` — 数据迁移

**文件路径**: `back/db/migrate_v3.py`

**`migrate_and_fill()`** — 旧 `content_index` → 新分表

6 个 `_insert_*` 函数，各负责一种内容类型的 UPSERT。自动补齐 API 标注为"必须"的字段默认值。

**`insert_user_behaviors()`** — 插入 6 个用户的 36 条模拟行为数据

---

### 3.11b `db/populate_vectors.py` — 向量预计算

**文件路径**: `back/db/populate_vectors.py`

**`populate_vectors()`** — 遍历 6 种内容类型，BGE 编码全部候选文本，写入 `content_vectors` 表

**`build_ivfflat_index()`** — 当数据量 >= 100 时重建 IVFFlat 索引（提升检索效率）

---

### 3.12 `sync/scheduler.py` — 定时任务

**文件路径**: `back/sync/scheduler.py`

**`start_scheduler()`** — 注册 2 个 APScheduler 定时任务:
- `content_sync`: 每 `SYNC_INTERVAL_CONTENT` 分钟执行 `sync_all_content()`
- `behavior_sync`: 每 `SYNC_INTERVAL_BEHAVIOR` 分钟执行 `sync_behavior()`

**`stop_scheduler()`** — 关闭调度器

**`run_all_sync_now()`** — 首次启动时执行全量同步（行为 → 内容）

---

### 3.13 `sync/content_sync_v2.py` — 内容同步

**文件路径**: `back/sync/content_sync_v2.py`

**`sync_all_content() -> dict`** — 遍历 4 种内容类型 (courses, training, skills, scales)，调用外部 API → UPSERT 到对应分表

**`_sync_single_type(api_type) -> dict`** — 单个类型同步，记录同步日志到 `sync_log`

---

### 3.14 `sync/behavior_sync_v2.py` — 行为同步

**文件路径**: `back/sync/behavior_sync_v2.py`

**`sync_behavior() -> dict`** — 从外部 API 拉取用户行为（browsing/search/learning/signup），写入 `user_behaviors` 表，注册信息写入 `user_profile`

---

### 3.15 `adapters/external_api.py` — 外部 API 适配

**文件路径**: `back/adapters/external_api.py`

**`fetch_api_data(api_type: str, token: str = "") -> dict`** — 调用外部接口获取数据:
- 请求头: JWT Bearer Token (支持传用户 Token 做权限隔离)
- `token` 为空时使用全局 JWT_TOKEN，非空时使用用户 Token
- 分页: pageSize=500, 最多5页
- 短时间请求合并到下一轮（通过 `WAIT_SECONDS` 配置）

**`fetch_all_apis(api_types, token="")`** — 并发调用多个接口

**`ExternalApiError`** — 外部 API 异常类

---

### 3.16 `schemas/recommend.py` — 请求/响应模型

**文件路径**: `back/schemas/recommend.py`

**`RecommendRequest`** — Pydantic 模型:
- `token` (str, 必填) — JWT Token，后端解析提取 user_id
- `context_type` (str, 默认 "browsing")
- `context_content_id` (str | None)
- `context_text` (str, 默认 "")
- `limit` (int, 默认 4, 范围 1-8)
- `content_types` (list[str] | None)

**`RecommendResponse`** — 嵌套模型，包含 `code`, `data: RecommendData`

**`RecommendItem`** — `content_id`, `content_type`, `title`, `reason`, `score`, `tags`

**`FeedbackRequest`** — `user_id`, `trace_id`, `event_type`, `content_id`, `content_type`, `position`

---

### 3.17 `api/llm_config.py` — LLM 厂商配置管理

**文件路径**: `back/api/llm_config.py`

**`VENDOR_PRESETS`** — 厂商预设配置:
- 硅基流动 (Qwen2.5-7B)、DeepSeek (flash)、通义千问 (qwen-plus)、Kimi (moonshot-v1)、智谱 (glm-4-flash)
- 每厂商预设 endpoint、默认模型名

**`GET /api/v1/config/llm`** — 获取当前配置 + 可用厂商列表，API Key 脱敏显示

**`POST /api/v1/config/llm`** — 保存配置到 `llm_config.json`，自动清 Redis 缓存避免新旧模型评分混淆

**`get_llm_config()`** — 供 `llm_scorer` 读取运行时配置

---

### 3.18 `db/redis_client.py` — Redis 异步连接

**文件路径**: `back/db/redis_client.py`

**`get_redis()`** — 获取全局异步 Redis 连接（延迟连接，首次调用时建立）

**`LLM_CACHE_TTL`** — 缓存过期时间 (默认 600 秒)

**缓存 Key 格式**: `llm_score:{user_id}:{content_type}:{candidates_fp}:{context_fp}`

**Value**: JSON 字符串 `{"content_id": {"s": 0.85, "r": "匹配原因"}, ...}`

---

### 3.19 用户权限隔离 (`user_content_visible` 表)

**数据库表**: `user_content_visible(user_id, origin_id, content_type, synced_at, PRIMARY KEY(user_id, origin_id))`

**写入时机**: 用户首次访问 (`REQUIRE_PERMISSIONS=true` 时)，用该用户的 JWT Token 调用 5 个内容接口

**使用方式**: `get_all_candidates` 通过 `INNER JOIN user_content_visible` 过滤，实现不同用户看到不同内容池

**调试隔离**: `REQUIRE_PERMISSIONS=false` 时完全跳过权限同步，推荐返回全量内容

---

### 3.17 `utils/enums.py` — 枚举定义

**文件路径**: `back/utils/enums.py`

- `ContextType`: BROWSING, SEARCH
- `BehaviorCategory`: BROWSING, SEARCH, LEARNING
- `ContentType`: COURSE, TRAINING, SKILL, SCALE, CAREER

### 3.18 `utils/retry.py` — 指数退避重试

**文件路径**: `back/utils/retry.py`

#### 公共函数

**`retry_async(fn, *args, max_retries=3, base_delay=2.0, retry_on=None)`**

异步函数重试器，默认只对 DB 瞬时故障重试。

| 参数 | 默认值 | 说明 |
|------|--------|------|
| fn | (必填) | 要重试的异步函数 |
| max_retries | 3 | 最大执行次数（含首次） |
| base_delay | 2.0 | 基础延迟秒数 |
| retry_on | None | 指定重试异常类型，默认自动检测 DB 错误 |

重试延迟: `base_delay × 2^(attempt-1)`，即 2s → 4s → 8s

#### 内部函数

**`_get_db_retry_errors() -> tuple`** — 延迟加载 7 种 DB 瞬时故障异常类型：
- `sqlalchemy.exc.OperationalError` — 连接断开、死锁
- `sqlalchemy.exc.TimeoutError` — 查询超时
- `sqlalchemy.exc.DisconnectionError` — 连接池失效
- `asyncpg.ConnectionDoesNotExistError` — 连接被关闭
- `asyncpg.TooManyConnectionsError` — 连接池满
- `asyncpg.ConnectionFailureError` — 无法建立连接
- `asyncpg.IdleSessionTimeoutError` — 空闲超时

---

## 4. 数据库表结构

### 4.1 内容表（严格对齐 API 接口字段）

#### `courses` — 课程 (atjCourses 接口)

| 字段 | API来源 | 必须 | 说明 |
|------|---------|------|------|
| origin_id | id | ✓ | 外部唯一标识 |
| num_xs | numXs | | 课程编号 |
| name | name | | 课程名称 |
| texts1 | texts1 | | 课程受众 |
| texts2 | Texts2 | ✓ | 课程收益 |
| texts3 | Texts3 | ✓ | 授课形式 |
| texts4 | Texts4 | ✓ | 课程大纲 |
| id | - | | SERIAL 主键 |
| status | - | | active / 停用 |
| sync_at | - | | 最后同步时间 |

#### `training_classes` — 培训班 (atjTraining 接口)

| 字段 | API来源 | 必须 | 说明 |
|------|---------|------|------|
| origin_id | id | ✓ | 外部标识 |
| snum | snum | ✓ | 班级编号 |
| name | name | ✓ | 班级名称 |
| classify | classify | | 所属类别 |
| start_apply | startApply | ✓ | 报名开始 |
| end_apply | endApply | ✓ | 报名截止 |
| start_study | startStudy | ✓ | 学习开始 |
| end_study | endStudy | ✓ | 学习截止 |
| limits | limits | ✓ | 限制人数 |
| regions | regions | | 适用区域 |
| places | places | | 培训地点 |
| training | training | ✓ | 培训内容 |
| certif | certif | | 证书 |
| deleted | deleted | | 正常/停用 |

#### `skill_positions` — 技能岗位 (atjSkills 接口)

| 字段 | API来源 | 必须 | 说明 |
|------|---------|------|------|
| origin_id | id | ✓ | |
| trade | trade | ✓ | 所属行业 |
| grade | grade | ✓ | 所属产业 |
| codes | codes | ✓ | 岗位编号 |
| name | name | ✓ | 岗位名称 |
| years | years | ✓ | 工作年限 |
| salary | salary | ✓ | 岗位薪资 |
| degree | degree | ✓ | 学历要求 |
| major | major | ✓ | 专业要求 |
| working | working | ✓ | 经验要求 |

#### `assessment_scales` — 测评量表 (atjScales 接口)

| 字段 | API来源 | 必须 | 说明 |
|------|---------|------|------|
| origin_id | id | ✓ | |
| product_id | productId | | 产品ID |
| name | name | ✓ | 量表名称 |
| tags | tags | | 标签 |
| pnums | pnums | | 题目总数 |
| ptimes | ptimes | ✓ | 平均用时 |
| texts1 | texts1 | ✓ | 测评介绍 |
| vals | values | ✓ | 适用人群 |

#### `career_plans` — 职业规划 (atjCareer 接口)

| 字段 | API来源 | 必须 | 说明 |
|------|---------|------|------|
| origin_id | id | ✓ | |
| name | - | | 从旧数据提取 |
| raw_data | - | | JSONB 兜底 |

> 接口文档中除 id 外其余字段开发中。

#### `news` — 资讯 (接口未就绪)

| 字段 | 说明 |
|------|------|
| origin_id | 唯一标识 |
| title | 标题 |
| raw_data | JSONB 兜底 |

### 4.2 系统表

#### `field_importance` — 字段重要性分级

| 字段 | 说明 |
|------|------|
| table_name | 所属表 (courses / training_classes / ...) |
| field_name | 字段名 |
| field_label | 中文名 |
| importance | 1-5 重要性等级 |
| description | 说明 |
| weight | 评分权重系数 |

#### `user_behaviors` — 用户行为

| 字段 | 说明 |
|------|------|
| user_id | 用户ID |
| user_name | 用户姓名 |
| behavior_type | browsing / search / learning |
| content | 行为内容文本 |
| extra_info | JSONB 扩展信息 |
| event_time | 行为时间 |

#### `user_profile` — 用户画像

| 字段 | 说明 |
|------|------|
| user_id | 主键 |
| base_tags | JSONB (education, major, skills, college, age_group, name) |

#### `content_vectors` — BGE 向量存储 (pgvector)

| 字段 | 说明 |
|------|------|
| content_id | 内容唯一标识 |
| content_type | 内容类型 |
| embedding | BGE 512维向量 (vector 类型) |
| cached_at | 缓存时间 |

索引: IVFFlat 索引 (`idx_content_embedding`)，用 `vector_cosine_ops` 支持余弦相似度快速检索。

向量在内容同步时预计算存储，推荐请求时只编码用户向量，通过 pgvector 的 `<=>` 算子做近邻搜索。

---

## 5. 定时同步机制

### 5.1 工作流程

```
FastAPI 生命周期
  │
  ├─ 启动时
  │   ├─ run_all_sync_now()
  │   │   ├─ sync_behavior()       → 拉接口 → user_behaviors + user_profile
  │   │   └─ sync_all_content()    → 拉接口 → 各分表 UPSERT
  │   │       └─ refresh_vectors_for_all()  → BGE编码 → content_vectors 表
  │   │
  │   └─ start_scheduler()
  │       ├─ 内容同步: 每 30 分钟 → 同步后自动刷新向量
  │       └─ 行为同步: 每 5 分钟
  │
  └─ 关闭时
      └─ stop_scheduler()
```

**同步步骤**:
1. `fetch_api_data(api_type)` — 调外部接口，分页拉取
2. 根据 api_type 分发到对应分表做 UPSERT
3. 内容指纹对比（`content_hash` 列）：计算新数据 MD5 → 与已存相比
   - 新增 → 标记 changed，写入 + 刷新向量
   - 变化 → 标记 changed，写入 + 刷新向量
   - 未变 → 跳过向量刷新（内容完全一样无需重新编码）
4. `refresh_vectors_by_ids()` — 仅对标记 changed 的内容编码 BGE 向量 → 写入 `content_vectors`
5. 记录同步日志到 `sync_log` 表

### 5.2 故障区分与重试

两类故障通过不同的机制区分：

**DB 瞬时故障** — 通过异常类型识别，触发指数退避重试：

```
哪些异常算 DB 瞬时故障（共 7 种）：
  sqlalchemy.exc.OperationalError        ← 连接断开、死锁
  sqlalchemy.exc.TimeoutError            ← SQL 执行超时
  sqlalchemy.exc.DisconnectionError      ← 连接池连接失效
  asyncpg.ConnectionDoesNotExistError    ← 连接被服务端关闭
  asyncpg.TooManyConnectionsError        ← 连接池耗尽
  asyncpg.ConnectionFailureError         ← 无法建立新连接
  asyncpg.IdleSessionTimeoutError        ← 空闲连接被回收

重试策略：
  第 1 次失败 → 等 2 秒 → 重试
  第 2 次失败 → 等 4 秒 → 重试
  第 3 次失败 → 等 8 秒 → 重试
  3 次全失败 → 放弃，记录 sync_log，等下个定时周期

非 DB 异常（如 JSON 格式错误、内存不足）不触发重试。
```

**外部 API 故障** — 通过代码位置隔离，不进入重试：

```
_sync_single_type() 的执行顺序：
  │
  ├─ fetch_api_data(api_type)     ← API 调用在 retry_async 外面
  │     │
  │     └─ 如果抛 ExternalApiError → 被外层 except 捕获
  │          → 直接记 sync_log → 不重试
  │          → 跳过当前类型，继续下一个
  │
  └─ retry_async(_write_to_db)    ← DB 写入在 retry_async 里面
        │
        └─ 如果抛 DB 瞬时故障 → 自动重试 3 次
```

**为什么 API 故障不重试？**

供应商接口返回 500 说明服务端有问题，等 2 秒大概率好不了。重试不仅白费力气，还可能加重对方服务器压力。不如等下一个定时周期（5-30 分钟后再试）。

### 5.3 重试模块

**文件**: `utils/retry.py`

**`retry_async(fn, *args, max_retries=3, base_delay=2.0, retry_on=None)`**

- 默认只重试 DB 瞬时故障（7 种异常类型），其他异常直接抛
- 指数退避: 延迟 = base_delay × 2^(attempt-1)，即 2s → 4s → 8s
- 日志记录每次重试的异常类型和原因

**`_get_db_retry_errors()`** — 延迟加载 DB 异常类型（避免无 SQLAlchemy 时启动报错）

---

## 6. 冷启动策略

**触发条件**: 用户无专业、无技能、无行为记录

**处理流程**:
1. 每种内容类型拉全量候选
2. 计算每条内容的字段完整性得分
3. 按得分降序取 top-N
4. 使用模板推荐理由
5. **不调用 LLM**（无用户上下文可匹配）

---

## 7. 降级与容错

| 故障场景 | 处理方式 |
|---------|---------|
| LLM 返回 503 / 超时 | 熔断器进入 OPEN，走降级公式 `BGE×0.5 + 字段×0.35 + 时间×0.15` |
| LLM JSON 解析失败 | 抛 LLMError → 降级 |
| 熔断器 30 秒后 | HALF_OPEN 试探一次，成功则恢复 CLOSED |
| 外部 API 同步失败 | 记录到 sync_log，下次定时继续重试 |
| 数据库异常 | HTTP 500 返回给前端 |
| 候选内容为空 | 返回空数组 |
| 初筛后 0 条 | 直接返回空数组 |
| 最终分 < 0.4 | 丢弃不返回 |
| 旧内容表迁移数据 | 自动补齐缺失的必须字段默认值 |

---

## 8. 踩坑记录与经验总结

### 8.1 asyncpg 不兼容 pgvector 的 `::vector` 类型转换

**现象**: skill(58条) 和 scale(76条) 推荐返回空，日志报错:
```
asyncpg.exceptions.PostgresSyntaxError: invalid input syntax for type vector: "[-0.048353,..."
```

**排查过程**:
1. 打开对应类型的推荐页面 → 显示"暂无推荐"，后端日志有报错但服务没挂
2. 先怀疑是向量编码格式问题 → 单独在 psql 里跑同样的 `[...]::vector` SQL → 正常返回，说明向量格式本身没问题
3. 注意到只有 skill 和 scale 报错，course/training/news/career 都正常 → 排除全局性故障
4. 回头看 `pre_screen_candidates` 函数，发现第 157 行有个早退：`if len(candidates) <= 30: return all_candidates`。skill(58条) 和 scale(76条) 超 30 走了 `vector_search`，其他类型全早退跳过了 → 锁定 bug 就在 `vector_search`
5. `vector_search` 用 SQLAlchemy async (`text()` + `:param` 绑定) 拼 SQL，确认是 asyncpg 的 prepared statement 缓存不认 `::vector` → 同样的 SQL 在 psycopg2 和 psql 下全部正常
6. 最终改用 psycopg2 同步连接执行向量查询，和已有的向量写入保持一致

**根因**: asyncpg 的 prepared statement 缓存不兼容 pgvector 扩展的 `::vector` 类型转换。

**修复**: `vector_search` 改用 psycopg2 同步连接:
```python
conn = psycopg2.connect("dbname=recommend_agent host=/tmp")
cur.execute("SELECT ... WHERE ... ORDER BY embedding <=> %s::vector", (vec_str, ...))
```

### 8.2 `await` 遗漏：coroutine 对象被当 list 使用

**现象**: 所有类型推荐同时返回空，日志报错:
```
object of type 'coroutine' has no len()
```

**排查过程**:
1. `orchestrator_v2.py` 中 `_recommend_for_type` 被外层 `except Exception` 捕获并吞掉了异常
2. 看错误消息 `has no len()` → 说明代码在拿一个不是 list 的对象做长度检查
3. 追踪 `candidates` 的来源：来自 `pre_screen_candidates()` 的返回值 → 但返回的是 coroutine 对象，不是 list
4. 回到调用处发现 `candidates = pre_screen_candidates(...)` — 漏了 `await`
5. Python 的 async 函数不加 `await` 不会报语法错误，默默返回一个 coroutine 对象，后续代码当 list 用才崩

**根因**: `orchestrator_v2.py` 调用 `pre_screen_candidates()` 漏写 `await`。

**教训**: async 函数错过 `await` 是静默错误——Python 不警告，只在后续访问时崩溃。可以启用 mypy/pyright 类型检查来提前发现。

### 8.3 行为记录表无限膨胀（2620 → 45 条）

**现象**: 查看数据库时发现 `user_behaviors` 表有 2620 条记录，但测试只有 6 个用户 × 6 条行为 = 36 条。

**排查过程**:
1. `SELECT user_name, behavior_type, content, count(*) FROM user_behaviors GROUP BY 1,2,3 HAVING count(*) > 1` → 高权忠的 10 种行为各重复 235 次（10 × 235 = 2350 条冗余）
2. 重复次数和运行时间吻合：235 ≈ 同步次数。说明 `behavior_sync_v2.py` 每 5 分钟跑一次 INSERT，每次都给同一个用户插入同样的行为，从未检查已经存在
3. 数据库没有唯一约束，代码层也没有去重

**根因**: 行为同步每 5 分钟无脑 INSERT，没有去重。

**修复**:
1. `ALTER TABLE user_behaviors ADD COLUMN origin_id VARCHAR(64) UNIQUE` — 存 API 返回的行为记录 id
2. SQL 改为 `ON CONFLICT (origin_id) DO NOTHING` — 与内容表使用同一套去重逻辑
3. API 文档中 id 为必须字段，不需本地生成
4. 清理旧数据: 2620 → 45 条

### 8.4 内容同步每次全量重编码向量

**现象**: `sync_all_content` 每次跑完就调用 `refresh_vectors_for_all`，不管外部接口有没有返回新数据，161 条内容全部 BGE 编码一遍。BGE 编码是 CPU 密集操作（单条 ~5ms），每 30 分钟全量重算一次浪费严重。

**排查过程**:
1. 看 `sync_all_content` 的流程 → 拉 API → 写入 → `refresh_vectors_for_all`。最后一步无条件执行，接口返回 0 条新数据也照样刷新
2. 理想行为：只有内容确实变了才需要新向量。但如何判断"变了"？
3. 方案对比：存整个原始 JSON 再逐字段对比太笨重 → 选 MD5 指纹，只对核心文本字段做哈希
4. 需要存指纹到数据库以便下次对比 → 6 张内容表各加 `content_hash` 列

**根因**: 没有区分"API 返回了但内容未变"和"API 返回了且内容变了"。

**修复**:
1. 6 张内容表增加 `content_hash VARCHAR(64)` 列
2. 同步时: 计算新数据的 MD5 → 与 DB 中的 `content_hash` 比较
3. 指纹相同 → 不刷新向量；指纹不同或不存在 → 标记 changed，只刷新这几条
4. 首次为已有数据执行 `UPDATE ... SET content_hash = md5(...)` 一次性生成

### 8.5 LLM 替换数据库连接方案引入异步陷阱

**现象**: 从 SQLAlchemy 异步切换到 pyscopg2 同步后，没有考虑到 pyscopg2 的 `connect()` 和 `execute()` 是同步阻塞调用。

**处理**: `vector_search` 虽用了 psycopg2，但 psycopg2 的查询非常快（几毫秒），在协程中同步调用不会显著阻塞事件循环。真正耗时的 BGE 编码放在 `asyncio.to_thread()` 中执行。如果将来查询量增大，可以改为 `run_in_executor` 包装。

### 8.6 IVFFlat 索引在小数据量下收益不明显

**现象**: IVFFlat 索引已创建（`lists=10`），向量检索走的是 PostgreSQL 的 `<=>` 算子 + pgvector 索引路径，并非旧版的内存暴力 for 循环。但当前数据总量 161 条，IVFFlat 要求最低 `lists × 30 = 300` 条才能发挥价值。几十条数据时，PostgreSQL 查询优化器可能直接选全表扫——因为树索引的节点跳转开销和顺序扫几十行的代价差不多。

**当前状态**: 索引已建好，基础设施就位。数据量增长到几百条后自动生效，届时无需任何代码改动。如需进一步优化可升级为 HNSW 索引单次查询延迟从毫秒压缩到微秒。

### 8.7 数据库连接路径的隐蔽性

**问题**: 项目使用本地编译安装的 PostgreSQL，socket 在 `/tmp/.s.PGSQL.5432`，而系统默认 PostgreSQL 客户端在 `/var/run/postgresql/`。psycopg2 默认也连 `/var/run/postgresql`。

**解决**: 所有 psycopg2 连接统一使用 `host=/tmp` 参数指定 socket 目录。

### 8.8 BGE 编码阻塞事件循环

**现象**: 并发压测中 p90 延迟随并发数线性增长，虽然零失败，但单次 BGE 编码（~300ms）时主线程事件循环被独占。

**根因**: ① `encode_text()` 是同步 CPU 密集调用 ② BGE 默认跑在 CPU 上，30 条文本编码需 326ms。

**修复（两阶段）**:
1. 新增 `encode_text_async()` / `encode_batch_async()`，用 `asyncio.to_thread()` 将 BGE 编码扔到线程池，避免阻塞事件循环
2. 改为 GPU 自动检测（`torch.cuda.is_available()`），有 GPU 优先用 GPU：30 条文本从 326ms 降至 11ms（30× 提速）
3. 无 GPU 时自动回退 CPU

```python
# engine/embedding.py
def _detect_device() -> str:
    try:
        import torch
        if torch.cuda.is_available():
            return f"cuda:{torch.cuda.current_device()}"
    except ImportError: pass
    return "cpu"
```

### 8.9 内容向量表与内容表不一致（事前预防 + 事后修复）

**现象**: 同步时先写内容表成功，但向量写入中途服务挂了，会出现"有内容但没向量"的孤儿数据。
用户推荐请求会因查不到向量而跳过该内容，导致数据丢失而不自知。

**排查过程**:
1. 发现 content_sync 是先写分表（SQLAlchemy async session → commit）再单独写向量表（psycopg2 另一条连接 → commit）。两步之间没有事务保护。
2. 验证：手动检查 `content_vectors` 和分表的 origin_id 对应关系 → 发现 28 条 orphan 数据
3. 问题本质：异步写入 + 独立连接 = 无法用数据库事务包裹。跨库事务太重，不适合这场景
4. 最终选标记方案：在内容表上加 `has_vector` 字段，写入成功后才标 TRUE，推荐时过滤掉 FALSE 的

**事前预防 — has_vector 标记**:
1. 6 张内容表增加 `has_vector BOOLEAN DEFAULT FALSE` 列
2. `get_all_candidates` 增加过滤 `WHERE has_vector = TRUE`
3. 同步链路: 内容变了 → `has_vector=FALSE` → 刷新向量 → `has_vector=TRUE`

**事后修复 — repair_missing_vectors**:
1. `get_missing_vector_ids(content_type)` — 查询 `has_vector=FALSE` 的 origin_id 列表
2. `repair_missing_vectors(content_type)` — 补生成向量，写入后设为 TRUE

### 8.10b 推荐反馈未被利用（点击偏好反馈闭环）

**现象**: 推荐曝光/点击日志在 `recommend_log` 表里存着但没有任何分析利用，推荐结果不随用户行为变化。

**修复**: 
1. 新增 `get_user_click_preferences(user_id)` — 查询用户历史点击分布
2. 评分公式中增加 `click_boost = min(click_count × 0.015, 0.05)`
3. 用户对某种类型点得越多，该类型推荐微弱加分（最高+0.05）
4. 轻量级反馈闭环，不改变主要排序逻辑

### 8.11 has_vector 标记被误清零（8.9 的延续）

**现象**: 之前修好了向量一致性问题（8.9），但几轮定时同步后，skill(58条) 和 scale(76条) 的推荐又全部返回空。

**排查过程**:
1. `SELECT has_vector, count(*) FROM skill_positions WHERE status='active' GROUP BY 1` → 全部 FALSE
2. 谁在清这个标记？只有 `DO UPDATE SET has_vector=FALSE` 会清
3. 查看 sync 日志 → 内容同步周期在数分钟前跑过 → 所有 API 返回的记录都被 DO UPDATE 处理，标记全清
4. 但内容实际没变（指纹没变），没走 `refresh_vectors_by_ids` → has_vector 停留在 FALSE
5. 问题非常明确：**DO UPDATE 不加条件地对所有记录重置标记**。没变的记录既被清了标记，又不会刷新向量恢复

**根因**: `DO UPDATE SET has_vector=FALSE` 对内容没变的记录也执行了，只清不复原。

**修复**:
1. 从 DO UPDATE 中移除 `has_vector=FALSE`
2. 改为在 `sync_all_content` 中，只对 `changed_ids` 显式执行 `UPDATE SET has_vector = FALSE`
3. 然后 `refresh_vectors_by_ids` 写回向量 → `UPDATE SET has_vector = TRUE`
4. 没变的记录全程不动 has_vector

**正确链路**:
```
同步一条记录:
  ├─ 指纹变了 → UPDATE has_vector=FALSE → 刷新向量 → UPDATE has_vector=TRUE
  └─ 指纹没变 → 不动 has_vector（保持 TRUE）
```

### 8.12 经验教训汇总

| # | 教训 | 避免方法 |
|---|------|---------|
| 1 | asyncpg 的 prepared statement 不兼容所有 PostgreSQL 扩展 | 遇到扩展类型优先用 psycopg2 测试；如果性能关键，考虑关闭 prepared statement |
| 2 | async 函数不加 await 不会报错，只在后续使用时崩溃 | 启用 pyright/mypy 类型检查；代码审查重点检查 async 函数调用 |
| 3 | 定时同步任务必须有去重机制 | 数据库加唯一约束兜底；代码层做 dedup |
| 4 | "全量刷新"模式在数据增长时会成为性能瓶颈 | 引入内容指纹做增量更新 |
| 5 | 自定义 PostgreSQL 安装路径需要显式指定 socket | 统一管理连接参数，使用配置文件而非硬编码 |
| 6 | 向量索引在小数据集上效果不明显 | 先暴力扫描，数据量达标后再建索引 |
| 7 | DO UPDATE 中不加条件地对所有记录重置标记会导致"全盘清零" | UPDATE 只在内容实际变化时才动标记；先 FALSE 再 TRUE 的两阶段操作确保闭环 |
| 8 | 只有事前预防不够，还需要事后修复兜底 | has_vector 标记做事前预防，repair_missing_vectors 做事后修复 |
| 9 | 跨连接的 upsert + 标记更新需要仔细编排执行顺序 | 先置 FALSE → 写向量 → 置 TRUE 三步走，任何一步失败都能在下个周期修复 |
| 10 | IVFFlat 索引参数不会自动适配数据分布，低密度类型被跳过 | 根据各类型数据量选取合适的 lists 和 probes；每次新增批量数据后验证各类型是否能在 LIMIT 下返回结果 |
| 11 | `pre_screen_candidates` 候选≤30 时不排序，截断误删高分内容 | LLM 候选截断前先 `sort(key=_sim, reverse=True)` 确保最高相似度内容优先送入 LLM |
| 12 | DeepSeek 并发 6 请求时 3 个返回空 content，降级泛滥 | Semaphore(1) 串行 + top 8 截断 + 1 次重试，降级率从 53% 降至 24% |
| 13 | 权限表残缺数据（空壳课程）通过 JOIN 阻断真课程推荐 | `REQUIRE_PERMISSIONS` 开关只生产触发同步；同步加 `≥4/5` 完整性校验；按 content_type 粒度检查权限 |

### 8.12a IVFFlat 索引探针不足导致低密度内容类型新向量被忽略

**现象**: 新增 9 条量表（706-708），BGE 相似度测试正常（sim=0.62~0.68），但刘强（机械设计背景）的推荐结果中量表部分只有 1 条"创新创业能力测评"（score=0.17），新增的机械相关量表一条都出不来。日志显示 `[V5] scale: 初筛 62 → 7`，而期望值是 62→30。

**排查过程**:
1. 确认 706-708 向量已生成且 `has_vector=TRUE` ✓
2. 确认 `content_vectors` 表中 706-708 的 embedding 存在 ✓
3. 用 pgvector `<=>` 算子直接计算 BGE 相似度：706=0.63, 707=0.66, 708=0.68 → 都远高于 SIMILARITY_THRESHOLD(0.45) ✓
4. 用实际请求的 user_vector_text 重跑 `vector_search` SQL：LIMIT 30 返回 0 行；LIMIT 60 返回 60 行（包含 706-708）⚡
5. `SET enable_indexscan=off`（禁用 IVFFlat 索引）：LIMIT 30 立即返回 30 行，706-708 全部出现 ✓

**根因**: IVFFlat 索引参数 `lists=10` + 默认 `probes=1`，只扫描最近的 1 个聚类的 10% 数据。量表数据量少（62 条），且和课程/技能等其他类型的向量分布在不同聚类中。探针扫到的前几个聚类碰巧都是其他类型，过滤 `content_type='scale'` 后在 LIMIT 30 内凑不够行数，直接返回空。

```
IVFFlat 索引结构 (lists=10):
  聚类0: [课程向量×8, 技能向量×3, 新闻向量×2]    ← 探针扫这里
  聚类1: [技能向量×5, 量表向量×4]                 ← 跳过了
  聚类2: [课程向量×7, 量表向量×3]                 ← 跳过了
  聚类3: [已扫] → 聚类4: [跳过] → ...
  聚类5: [量表向量×6, ...]                        ← 这里才有量表
  ...但 probes=1，只扫聚类0，LIMIT 30 内 scale 为 0 条
```

**修复**:
1. 重建索引：`lists=5`（每个聚类约 32 条，覆盖更多同类型）
2. 增加探针：`SET ivfflat.probes = 3`（扫描 3/5 = 60% 数据，对于每类型 60-80 条的数据量，足够返回 LIMIT 30）
3. `vector_search` 函数每次查询前主动设置 probes

修复后效果：初筛 62→30，刘强的量表推荐从 1 条（0.17）变为 4 条（0.73~0.79），新增量表全部出现。

**教训**: IVFFlat 不是"开箱即用"的——`lists` 和 `probes` 需要根据每类型数据量调参。经验公式：`probes ≥ lists × LIMIT / 该类型向量总数`。每新增批量数据后应验证各类型是否能在当前参数下返回预期数量。

### 8.12b BGE 初筛未排序导致 LLM 截断误删高分候选

**现象**: 王芳（护理学）的课程推荐出现"机械制图与CAD设计"而非"医疗护理基础"。检查 Redis 缓存发现 LLM 对所有 8 条候选打分 0.05（均为不相关），但 BGE 单独测试显示"医疗护理基础"的相似度高达 0.81（排名第 1）。

**排查**:
1. BGE 相似度验证：`医疗护理基础 sim=0.81, 人力资源管理 sim=0.54, …` → 医疗护理基础确实最高分 ✓
2. Redis 缓存 keys 查看：LLM 评分的候选列表为 `c001, c002, c003, c004, …`（电工基础, PLC编程, Python数据分析, 财务管理…），没有 c009 ✓
3. 追踪 `pre_screen_candidates` 返回值：候选数 20 ≤ max_keep(30) → 触发早期 return，直接返回数据库排序结果（ORDER BY id），**不按 `_sim` 排序**

**根因**: 两种路径的行为不一致：

```python
# 路径 A: 候选 > 30 (如 scale → 56条)
relevant.sort (by _sim) → result = relevant[:23] + unhit[:7] → 已排序 ✓

# 路径 B: 候选 ≤ 30 (如 course → 20条)
if len(all_candidates) <= max_keep:
    return all_candidates  # 直接返回，数据库 ORDER BY id 顺序，未排序 ✗
```

路径 B 返回 `c001, c002, c003, ..., c020`。而 `orchestrator_v2` 执行 `candidates[:8]` 截取前 8 条 → 拿到 c001~c008（电工基础到人力资源），c009 医疗护理基础被截断。

**修复**: 在 orchestrator_v2 截断 LLM 候选前加一行排序：
```python
candidates.sort(key=lambda x: x.get("_sim", 0), reverse=True)
llm_candidates = candidates[:8]
```

### 8.12c DeepSeek API 并发返回空 + 硅基流动 7B 小模型 JSON 不稳定

> 本节记录了 LLM 供应商稳定性问题和一次失败的模型迁移尝试。

**现象**: 恢复 JWT 鉴权后，高权忠的 6 类推荐中 course/scale/skill 三类全部降级（"根据您的背景为您推荐该…"），只有 news/training/career 有 LLM 个性化理由。降级率 53%。

**排查过程**:
1. 确认 `DISABLE_LLM=False` ✓
2. 确认 API Key 正确（`llm_config.json` 中为真实 key）✓
3. 查看日志：`LLM 返回空 content! type=scale, prompt_size=14505` → LLM HTTP 200 但 body 中 content 为空字符串
4. prompt 大小排查：scale=14505chars（30条候选），course=3303chars，skill=3645chars → 怀疑超 token 限制
5. 去重试/去 Semaphore → 回滚到昨天正常版本的代码 → **依然失败**

**结论**: 非代码问题，DeepSeek API 当天不稳定，同一天同一代码昨天成功今天失败。候选数大的类型（scale 30条→14505chars）失败率更高。

**修复**:
1. LLM 候选截断为 top 8（`candidates[:8]` + 按 `_sim` 排序），prompt 从 14505 → ~5000chars
2. `asyncio.Semaphore(1)` 串行调 LLM，避免并发下不稳定
3. 1 次重试（2 次尝试），3s 延迟
4. 降级率从 53%（10/19）降至 24%（4/17），仅 scale 偶发失败

**硅基流动 Qwen2.5-7B 测试** (2026-07-31):
- 尝试切换硅基流动 `Qwen/Qwen2.5-7B-Instruct` 模型，HTTP 200 但 JSON 被 markdown 代码块包裹 (`\`\`\`json`)
- 添加 markdown 剥离后仍有 JSON 语法错误（缺少逗号分隔符），7B 小模型生成结构化 JSON 不稳定
- 结论：切换回 DeepSeek flash，保留 markdown 剥离代码兼容性

### 8.12d 权限表脏数据导致真推荐被阻断

**背景**: V3 新增 `user_content_visible` 表（user_id × origin_id 联合主键），实现用户级内容权限隔离。推荐时 `get_all_candidates` 通过 `INNER JOIN` 该表过滤可见内容。

**问题 1 — 残缺数据污染（连续触发 3 次）**:

| 轮次 | 现象 | 原因 |
|------|------|------|
| 第 1 次 | course 只剩 3 条（origin_id 1,2,3，全空壳），真课程 c001-c020 消失 | `_sync_user_permissions` 用测试 Token 调外部接口，course 只返回 3 个空 placeholder |
| 第 2 次 | 清掉后重启，又被填回来，course/training/career 全空 | `_ensure_user_exists` 检查"用户有记录→跳过"失效——清理的是该用户记录，`__disabled__` 占位行是另一个 `user_id`，各管各的 |
| 第 3 次 | 同上 | 同上 |

**根因分析**:

```
测试 Token → 外部 API → 部分类型返回空壳数据 → _sync_user_permissions 全量存储
                                                      ↓
user_content_visible: shty80001 / course / [1,2,3] (3条空壳)
                      shty80001 / scale  / [62条] (正常)
                      shty80001 / skill  / [55条] (正常)

get_all_candidates JOIN → course 只拿到 3 条空壳 → BGE 全 0.0 → LLM 全 0 → 结果 0 条
```

**问题 2 — 全局检查 vs 类型粒度**:

```python
# 「是否有权限记录」的检查使用了全局 LIMIT 1：
chk = SELECT 1 FROM user_content_visible WHERE user_id = :uid LIMIT 1
# → shty80001 有 scale/skill 记录 → True
# → course 没有记录 → 也走 JOIN → 0 行

# 修复: 加上 content_type
chk = SELECT 1 FROM user_content_visible WHERE user_id = :uid AND content_type = :ct
# → course 没有 → has_perms=False → 不走 JOIN → 返回全量
```

**修复方案（三道防线）**:

1. **生产才同步**: `_ensure_user_exists` 只在 `REQUIRE_PERMISSIONS=true` 时才调用 `_sync_user_permissions`。调试环境 (`false`) 跳过，永久避免脏数据。
2. **完整性校验**: 同步时检查 ≥4/5 类型有数据才算成功，残缺结果直接丢弃不写入。
3. **按类型粒度检查**: `get_all_candidates` 的权限检查加上 `AND content_type = :ct`，一个类型没有权限不影响其他类型。

**教训**: 权限表是推荐的前置过滤层，SQL 层的 `INNER JOIN` 会直接掐断候选管道。脏数据在用户看不到之前不会暴露——BGE/LLM 根本收不到真内容。调试环境必须用 `REQUIRE_PERMISSIONS=false` 隔离权限同步逻辑。

### 8.13 LLM 评分 Redis 缓存

**背景**: LLM API 调用耗时 ~1.4s，用户短时间内重复点击同一 tab 会重复调用 DeepSeek，造成 API 费用浪费和响应延迟。

**设计**:

```
缓存层位于 orchestrator_v2._recommend_for_type() 的 LLM 调用前。

Key: llm_score:{user_id}:{content_type}:{candidates指纹}:{上下文指纹}
Value: JSON 字符串 → {"content_id": {"s": 0.85, "r": "匹配原因"}, ...}
TTL: 600 秒 (10 分钟)

候选指纹 = MD5(排序后的候选 content_id 拼接)
上下文指纹 = MD5(user_context 文本, 含画像+行为+当前浏览)
```

**命中/失效机制**:

```
① 用户行为变了 → user_context 不同 → 上下文指纹不同 → miss
② 用户画像变了 → user_context 不同 → 上下文指纹不同 → miss
③ 新内容入库了 → 候选列表不同 → 候选指纹不同 → miss
④ 用户切换 tab → content_type 不同 → key 不同 → miss
⑤ 同一用户同一上下文再次请求 → 所有成分一致 → HIT → 跳过 LLM
```

**降级**: Redis 不可用时静默跳过缓存，走正常的 LLM 调用路径，缓存写入失败也不影响主流程。

**依赖**: `redis-py` (版本 8.0+，内置 `redis.asyncio` 异步客户端)，Redis 7.2 本地编译。

**效果**: 同一用户连续点击相同内容 → 响应从 8.8s 降至 0.19s（缓存命中时仅 BGE 编码 ~140ms + DB 查询 ~30ms）。

---

## 9. 性能测试

> 测试环境: 384核 + 4×RTX 4090, BGE-small-zh (CPU), 189条内容(含新增9条量表), PG max_connections=300, Redis 7.2 缓存
> 测试方式: Python asyncio 并发发包, 关闭 LLM (纯本地计算)

### 9.1 并发-延迟测试

**CPU 环境**（开发机，LLM 关闭，纯本地计算）：

| 并发数 | QPS | 平均延迟 | p50延迟 | p90延迟 | p99延迟 | 失败数 |
|--------|-----|---------|--------|---------|---------|--------|
| 10 | 15.7 | 542ms | 509ms | 630ms | 705ms | 0 |
| 20 | 16.0 | 1067ms | 1124ms | 1242ms | 1250ms | 0 |
| 50 | 15.9 | 2452ms | 2443ms | 3024ms | 3141ms | 0 |

**GPU 环境**（384核 + RTX 4090，LLM 关闭，纯本地计算，psycopg2 连接池 + `run_in_executor`）：

| 并发数 | QPS | 平均延迟 | p50延迟 | p90延迟 | p99延迟 | 失败数 |
|--------|-----|---------|--------|---------|---------|--------|
| 10 | 9.4 | 987ms | 1044ms | 1063ms | 1063ms | 0 |
| 20 | 8.6 | 2064ms | 2165ms | 2315ms | 2316ms | 0 |
| 50 | 9.3 | 4676ms | 4941ms | 5325ms | 5368ms | 0 |

**优化后**（LLM 关闭，持续 8s 压测，PG `max_connections=300`）：

| 配置 | 并发数 | QPS | 平均延迟 | p50延迟 | p95延迟 | p99延迟 | 失败数 | CPU利用率 | PG连接 |
|------|--------|-----|---------|--------|---------|---------|--------|----------|--------|
| 8 worker, 256线程池, async 3+3, pgpool 2-6 | 32 | 738.3 | 43ms | 39ms | 65ms | 109ms | 0 | ~5% | 96 |
| 同上 | 64 | 803.4 | 79ms | 72ms | 116ms | 307ms | 0 | ~5% | 96 |
| 同上 | 128 | 818.9 | 154ms | 115ms | 414ms | 555ms | 0 | ~5% | 96 |
| 12 worker, 256线程池, async 6+6, pgpool 2-12 | 32 | 835.2 | 38ms | 34ms | 66ms | 99ms | 0 | ~5% | 78 |
| 同上 | 64 | 1068.7 | 60ms | 49ms | 108ms | 311ms | 0 | ~5% | 78 |
| 同上 | 128 | **1161.8** | 109ms | 101ms | 144ms | 350ms | 0 | ~5% | 78 |
| 16 worker, 128线程池, async 4+4, pgpool 2-8 | 32 | 944.2 | 34ms | 31ms | 53ms | 84ms | 0 | ~5% | 102 |
| 同上 | 64 | 1409.8 | 45ms | 40ms | 65ms | 135ms | 0 | ~5% | 102 |
| 同上 | 128 | 1459.9 | 86ms | 77ms | 120ms | 419ms | 0 | ~5% | 102 |
| 同上 | 256 | **1538.3** | 164ms | 111ms | 457ms | 821ms | 0 | ~5% | 102 |
| 32 worker, 64线程池, async 3+3, pgpool 2-3 | 128 | 1558.9 | 81ms | 56ms | 210ms | 399ms | 0 | ~5% | 102 |
| 同上 | 256 | **1574.9** | 159ms | 91ms | 518ms | 711ms | 0 | ~5% | 102 |

> **QPS 天花板**: 1575（32 worker）。从 8→32 worker 后不再线性增长，CPU 始终 ~5% idle，PG 仅用 102/300 连接。瓶颈转移至 FastAPI 框架开销和异步事件循环调度，已超出应用层可控范围。

**最终验证**（16 worker × 128 线程池, LLM 关闭, 189 条内容含新增量表, PG max_connections=300, Redis 7.2）：

| 并发数 | QPS | 平均延迟 | p50延迟 | p95延迟 | p99延迟 | 失败数 | CPU利用率 |
|--------|-----|---------|--------|---------|---------|--------|----------|
| 32 | 864.8 | 37ms | 33ms | 64ms | 100ms | 0 | ~5% |
| 64 | 1194.4 | 52ms | 44ms | 75ms | 383ms | 0 | ~5% |
| 128 | 1590.9 | 80ms | 70ms | 116ms | 334ms | 0 | ~5% |
| 256 | **1647.1** | 153ms | 106ms | 377ms | 987ms | 0 | ~5% |

> **结论**: 189 条内容下峰值 QPS 稳定在 1647，较早前 161 条的 1538 略有提升（+7%），验证了架构的扩展性。全链路瓶颈始终在 I/O（DB 连接池和事件循环调度），CPU 384 核从未成为限制因素。

> **历史说明**: 早期 GPU 环境 QPS 反而不如 CPU（9.4 vs 15.7），原因是修复 `_sim` 后全类型走 `vector_search` 导致 psycopg2 每次新建连接开销×3。经过多轮优化（连接池 + 8 worker + 256 线程池 → 放宽 PG max_connections → 32 worker）将 QPS 从 9.4 拉升至 **1575**，提升约 **167 倍**。LLM 开启时延迟由 DeepSeek API（~1415ms）主导，QPS 峰值 ~437。

### 9.2 分析

**瓶颈演变过程**（LLM 关闭，纯本地计算）：

| 阶段 | 配置 | 峰值 QPS | 瓶颈 |
|------|------|----------|------|
| 1 | 单 worker, 默认32线程 | ~51 | psycopg2 每次新建连接 |
| 2 | 连接池 + run_in_executor | ~9 | 全类型 vector_search 连接×3 |
| 3 | 8 worker + 256线程池 | **819** | DB 连接池 (96/100) |
| 4 | 放宽 PG→300, 16 worker | **1538** | 事件循环调度 |
| 5 | 32 worker | **1575** | FastAPI/uvicorn 天花板 |
| 6 | + Redis LLM缓存, 189条内容 | **1647** | 同上, 新增内容无影响 |

- **最终 QPS**: 1647（LLM关闭），437（LLM开启），提升 **3.8×**
- **延迟**: p50 从 ~508ms（LLM）→ 70ms（纯本地），提升 **7.3×**
- **Redis 缓存效果**: 同一用户重复请求，LLM 开启时响应从 8.8s → 0.19s（**46×**加速）
- **零失败**: 所有阶段全部 0 错误
- **CPU 从未成为瓶颈**: 384 核始终用不到 6%
- **DB 连接池**: 放宽到 300 后实际使用峰值仅 102

### 9.3 BGE GPU vs CPU 性能对比

> 测试环境: 384核 + 4×RTX 4090，30条文本512维编码

| 模式 | 耗时 | 单条速度 | 倍数 |
|------|------|---------|------|
| CPU 串行 | 326ms | 10.9ms/条 | 1× |
| 8线程并行CPU | 538ms | 17.9ms/条 | 0.6× (反而更慢!) |
| **GPU 批量** | **11ms** | **0.4ms/条** | **30×** |

**分析**: 8线程 CPU 并行比串行还慢——因为多线程抢 CPU 时间 + GIL 上下文切换开销。GPU 批量推理 30 倍快，是唯一有效加速方案。

**当前状态**: `engine/embedding.py` 已改为自动检测 GPU，有 GPU 用 GPU，无 GPU 回退 CPU。

### 9.4 全链路延迟分解

**LLM 开启 + Redis 缓存命中**（第二次请求，缓存全命中）：

| 环节 | 耗时 | 占比 |
|------|------|------|
| asyncpg 业务查询 (画像/行为/候选/字段重要性) | ~30ms | 18% |
| BGE 编码 (CPU, 512维×6类型, 并发) | ~140ms | 82% |
| pgvector 向量查询 (×6类型) | ~1ms | <1% |
| LLM (DeepSeek API) | 0ms (跳过) | — |
| **合计** | **~171ms** | — |

**LLM 开启 + 首次请求**（缓存 miss）：

| 环节 | 耗时 | 占比 |
|------|------|------|
| asyncpg + BGE + pgvector | ~170ms | 11% |
| LLM (DeepSeek API × 6类型, 并发) | ~1415ms | 89% |
| **合计** | **~1585ms** | — |

**结论**: Redis 缓存消除 LLM 延迟后，BGE 编码成为主导因素（140ms/82%）。进一步优化可合并 6 类型 BGE 编码为 1 次 `encode_batch` 调用。

### 9.5 Embedding 模型区分度测试

| 指标 | BGE-small-zh (当前) |
|------|---------------------|
| 测试用例 | 10 对（3 个用户 × 跨专业内容） |
| 正确率 | 80% (8/10) |
| 正确组平均相似度 | 0.675 |
| 错误组平均相似度 | 0.389 |
| 区分度 gap | 0.286 |

正确组最低分 0.566 > 错误组最高分 0.449，两组之间没有交叉。说明 BGE-small 在当前内容库（161 条）的语义区分能力足够。如果内容库涨到万级、跨专业噪音变大，可考虑升级 GTE-base（768维，区分度提升约 5%，速度约 1.5x）。

### 9.6 本次部署环境（ARM64 容器）压测

> 测试环境：**ARM64 (aarch64) + 内核 4.19**，Docker 容器，纯 CPU 推理（BGE_FORCE_CPU=1），`torch.set_num_threads(1)`，`REQUIRE_PERMISSIONS=true`，140 条内容。
> 测试方式：ApacheBench (`ab -n N -c C`)，**缓存命中场景**（同参数，排除 LLM 耗时），POST `/api/v1/recommend`。

**优化前 vs 优化后（单请求 BGE 初筛耗时）**：
| 场景 | 单请求 | 20 并发 |
|------|--------|---------|
| 优化前（每类型重复编码用户向量）| 50ms | 73~290ms（3-6 倍退化）|
| 优化后（用户向量只编码一次复用）| **2ms** | 稳定 |

**QPS 与 worker 数关系（缓存命中）**：
| worker 数 | 并发20 | 并发50 | 失败 |
|-----------|--------|--------|------|
| 1 | 18.3 | - | 0 |
| 4 | 55.8 | - | 0 |
| 8 | **93.8** | - | 0 |
| 12 | 93.8 | 139.0 | 0 |
| 16 | - | 142.3 | 1 |

> **关键结论**：
> 1. **用户向量复用优化**：QPS 从 18 → 55（**3 倍**），初筛耗时 50ms → 2ms。
> 2. **worker 数瓶颈**：8 worker 时（并发20）到 93.8 基本封顶；12/16 worker 时需更高并发（-c 50）才能到 ~140，且 16 worker 相对 12 几乎无提升（142 vs 139）。
> 3. **QPS 受"并发度 + 共享资源"限制**：单请求缓存命中仅 ~7-10ms，需要足够并发才能填满 worker；worker 到 8-12 后瓶颈转移至数据库/Redis 连接池等共享资源，而非 CPU。
> 4. **最终配置定为 8 worker**（最优性价比）：93 QPS @并发20，内存 ~3.7GB，0 失败，稳定。
> 5. 对比开发机（384核+GPU）峰值 1647 QPS：本 ARM64 容器环境约 140 QPS，差异主要来自单核性能、纯 CPU 推理、容器网络、以及压测场景（本环境含缓存命中 + 权限过滤）。

---

## 10. Docker 部署踩坑记录与经验

> 本节记录 2026-08-04 将本项目从"单机开发"迁移到 "Docker Compose 部署" 时遇到的系列问题。
> 与第 8 章的"业务代码层"踩坑不同，本节集中在**部署/环境/容器化**层面。
> 部署目标机：ARM64 (aarch64) + 内核 4.19，Docker 28.3.3 + docker-compose v2.30.3。

### 10.1 Docker 权限问题（docker.sock）

**现象**: 执行 `docker info` / `docker ps` 报：
```
permission denied while trying to connect to the Docker daemon socket at unix:///var/run/docker.sock
```

**根因**: Docker CLI 是客户端，真正干活的是 daemon（`dockerd`），两者通过 Unix Socket（`/var/run/docker.sock`）通信。socket 默认属 `root:root`，权限 `srw-rw----`，其他用户无权限。标准做法是把用户加入 `docker` 组，但**即使加入了组，socket 的组还是 root，组权限也对不上**。

**解决**（临时）:
```bash
sudo chgrp docker /var/run/docker.sock   # 把 socket 组改成 docker
sudo chmod 666 /var/run/docker.sock      # 放开权限
```
> 注意：重启 Docker 后 socket 会被重建，组可能变回 root，临时方案会失效。

**解决**（长期，正规做法）: 改 systemd 的 docker.socket 单元文件，在 `[Socket]` 段加 `SocketGroup=docker`，然后：
```bash
sudo systemctl daemon-reload && sudo systemctl restart docker
```

**知识点**:
- `docker` 命令 = 客户端，`dockerd` = 守护进程。无权限访问 socket = 无法控制 Docker。
- 组成员身份在**登录时**写入进程（`/proc/self/status` 的 Groups 列表），加入 docker 组后**新开的终端**才生效，可用 `newgrp docker` 或重新登录。

---

### 10.2 `docker compose` vs `docker-compose`

**现象**: 执行 `./docker-build.sh` 报 `docker: unknown command: docker compose`。

**根因**: `docker-build.sh` 脚本写的是新版 `docker compose`（带空格，Docker 23+ 内置子命令），但目标机装的是**旧式独立命令 `docker-compose`**（带连字符）。

**解决**: 直接手动执行：
```bash
docker-compose build
docker-compose up -d
```

**知识点**:
- `docker compose`（空格）是 Docker 新版本内置的 Compose 插件；`docker-compose`（连字符）是独立的 Python 工具，两者命令格式完全一样，只是调用方式不同。
- 判断服务器用哪种：`docker compose version` 或 `docker-compose --version`。

---

### 10.3 内网环境下 Docker Hub 镜像拉不下来

**现象**: `docker-compose build` 报：
```
failed to resolve source metadata for docker.io/library/nginx:alpine
failed to do request: Head "https://docker.mirrors.ustc.edu.cn/...": no such host
```
或 `pull access denied ... insufficient_scope: authorization failed`。

**根因**: 这是**内网/受限网络**环境，无法直连 Docker Hub，且配置的镜像加速器（中科大、网易）已失效。其中 `authorization failed` 是因为配了 `registry.cn-hangzhou.aliyuncs.com`——阿里云的这个公共仓库**不是 Docker Hub 的镜像加速器**，它不代理 `docker.io/library/*` 官方镜像，所以拉官方镜像报无权限。

**解决**: 实测可用、能真正代理 Docker Hub 的加速器：
```json
{ "registry-mirrors": ["https://dockerproxy.net", "https://docker.1panel.live"] }
```
```
sudo systemctl restart docker
docker info | grep -A3 "Registry Mirrors"   # 确认生效
```

**知识点**:
- **镜像加速器（registry mirror）**：专门代理 docker.io 的镜像，配置在 `/etc/docker/daemon.json` 的 `registry-mirrors`。
- **阿里云公共仓库** `registry.cn-hangzhou.aliyuncs.com` 只是阿里云自己的仓库，**不是 Docker Hub 加速器**，不能代拉官方镜像。真正的阿里云加速器是 `https://<你的账号ID>.mirror.aliyuncs.com`（需账号）。
- 判断一个镜像源能不能用，不只是看 DNS 能否解析，还要测试能否返回镜像 tag 列表：
  ```bash
  curl -s https://<源>/v2/library/nginx/tags/list   # 返回 JSON 列表说明可用
  ```

---

### 10.4 ARM64 内核 bug 导致 Redis 启动即退出

**现象**: `docker-compose up -d` 时 redis 容器 Error，日志：
```
WARNING Your kernel has a bug that could lead to data corruption during background save.
Redis will now exit to prevent data corruption.
Note that it is possible to suppress this warning by setting the following config: ignore-warnings ARM64-COW-BUG
```

**根因**: 目标机是 **ARM64 (aarch64) + 老内核 4.19**。Redis 检测到该内核的 **COW（写时复制）bug**，后台 save 可能损坏数据，主动退出。

**解决**: 给 redis 加启动参数忽略警告：
```yaml
redis:
  image: redis:7-alpine
  command: ["redis-server", "--ignore-warnings", "ARM64-COW-BUG"]
```

**知识点**: Redis 7 在 ARM64 老内核上有 COW bug 检测，通过 `--ignore-warnings ARM64-COW-BUG` 可忽略（需评估数据安全风险）。

---

### 10.5 apt 包名不存在：`postgresql-client-16`

**现象**: 构建后端镜像时：
```
E: Unable to locate package postgresql-client-16
```

**根因**: Dockerfile 用 `python:3.12-slim`（基于 **Debian trixie/13**），该发行版的 PostgreSQL 客户端包名是 `postgresql-client`（不带版本号），不存在 `postgresql-client-16`。

**解决**: 改为通用包名 `postgresql-client`（让 apt 自动解析当前版本）。

**知识点**: 不同 Debian/Ubuntu 版本里 PostgreSQL 客户端包名不同。Debian 官方仓库通常只有 `postgresql-client`（跟随发行版自带版本），要装特定版本需加官方 PGDG 源。**在 Dockerfile 里应写发行版存在的包名**。

---

### 10.6 torch（155MB）下载超时

**现象**: 构建时 pip 下载 CPU 版 torch 到 ~66MB 就报 `Read timed out`，反复失败。

**根因**: torch 包很大（CPU 版 155MB），走 `download-r2.pytorch.org` 网络不稳，pip 默认 15 秒无数据就超时。

**解决**:
```dockerfile
RUN pip install --no-cache-dir \
    --index-url https://download.pytorch.org/whl/cpu \
    --timeout 300 --retries 5 \
    torch
```
（`--timeout 300` 延长读超时，`--retries 5` 失败重试；若仍失败可用 `wget -c` 断点续传先下到本地再 COPY 进镜像）

**知识点**: pip 大包下载超时用 `--timeout` + `--retries`；宿主机 `wget -c` 支持断点续传，比 pip 更抗网络抖动。

---

### 10.7 缺 `pgvector` Python 包

**现象**: 构建完成后 app 容器报 `ModuleNotFoundError: No module named 'pgvector'`。

**根因**: `populate_vectors.py` / `data_repo.py` 用了 `from pgvector.sqlalchemy import Vector`，但 `requirements.txt` **漏了 `pgvector` 包**。

**解决**: 在 `requirements.txt` 补充 `pgvector>=0.2.5` 后重新构建。

**知识点**: `pgvector` 有**两部分**：① PostgreSQL 扩展（数据库侧，`CREATE EXTENSION vector`，镜像用 `pgvector/pgvector:pg16` 自带）；② Python 包（应用侧，`pgvector`，需在 requirements 里装）。两者缺一不可。

---

### 10.8 entrypoint 连接池跨事件循环报错

**现象**: entrypoint 初始化时报：
```
RuntimeError: Task ... got Future attached to a different loop
```

**根因**: `get_db_session()` 返回的 SQLAlchemy async 连接池**绑定到第一个 `asyncio.run()` 的事件循环**。entrypoint 里分别用两次 `asyncio.run()` 跑 `migrate_and_fill()` 和 `insert_user_behaviors()`，第二次是新事件循环，从旧循环绑定的连接池取连接 → 报错。

**解决**: 把多个数据库操作**放进同一个 `asyncio.run()`** 里，共享一个事件循环：
```python
async def _init_data():
    await migrate_and_fill()
    await insert_user_behaviors()
asyncio.run(_init_data())
```

**知识点**: SQLAlchemy async engine 的连接池绑定创建时的事件循环，**同一个 async engine 不能在多个事件循环间复用**。多段初始化应合并到同一个 `asyncio.run` / 同一个 loop。

---

### 10.9 `build_ivfflat_index()` 参数不匹配

**现象**: `docker-entrypoint.sh` 调用 `build_ivfflat_index(lists=5)`，报 `got an unexpected keyword argument 'lists'`。

**根因**: entrypoint 传了 `lists=5`，但 `populate_vectors.py` 里 `build_ivfflat_index()` **定义不带参数**（内部已固定 `lists=10`）。

**解决**: 去掉 entrypoint 里的 `lists=5`，改为 `build_ivfflat_index()`。

**知识点**: 部署脚本与 Python 函数签名要保持一致。这类错误只在运行到那一步才暴露，属"脚本与代码耦合"问题。

---

### 10.10 `host=/tmp` socket 连接在容器内失败

**现象**: entrypoint / 同步时报：
```
psycopg2.OperationalError: connection to server on socket "/tmp/.s.PGSQL.5432" failed: No such file or directory
```

**根因**: 代码里多处硬编码 `psycopg2.connect("dbname=recommend_agent host=/tmp")`，这是**本机开发**时用 Unix socket 连本机 PostgreSQL 的写法。Docker 部署后 PostgreSQL 是**独立容器**，app 容器里的 `/tmp` 没有 postgres 的 socket 文件，必须走**网络 TCP**连接。

**解决**: 统一改为读取 `PGPOOL_DSN` 环境变量（`.env.docker` 已配 `host=postgres`），并保留 `host=/tmp` 作为本机开发回退：
```python
_PG_DSN = os.environ.get("PGPOOL_DSN", "dbname=recommend_agent host=/tmp")
conn = psycopg2.connect(_PG_DSN)
```
涉及文件：`back/db/populate_vectors.py`、`back/engine/data_repo.py`、`back/sync/content_sync_v2.py`。

**知识点**:
- `host=/tmp` = **Unix socket 连接**，只能连本机 PostgreSQL（socket 是本地文件）。
- `host=postgres` = **网络 TCP 连接**，在 Docker Compose 里 `postgres` 会被解析成 postgres 容器的 IP。
- 单机开发 vs 容器化部署，数据库从"同机"变"独立容器"，连接方式必须从 socket 改网络。

---

### 10.11 schema 缺失 `has_vector` 列 / `field_importance` / `content_vectors` 表

**现象**: app 初始化时报：
```
UndefinedColumnError: column "has_vector" does not exist
[SQL: SELECT * FROM courses WHERE status='active' AND has_vector = TRUE]
```

**根因**: 这是全新部署，但 `db/schema_v3.py` **只建了 7 张内容表**，缺少代码运行依赖的：
1. 每张内容表的 `has_vector BOOLEAN` 列（`data_repo.py` 查 `WHERE has_vector=TRUE`）
2. `field_importance` 表（`get_field_importance` 查询它，V2 里有但 V3 漏了）
3. `content_vectors` 表（向量存储，代码里 INSERT/SELECT 但没建表语句）

代码逻辑本身（查 `has_vector`、`UPDATE SET has_vector`）是项目原有的，只是**建表脚本漏建了表结构**。

**解决**: 在 `schema_v3.py` 补上：
1. 6 张内容表各加 `has_vector BOOLEAN NOT NULL DEFAULT FALSE`
2. 补 `field_importance` 表 + 预置数据（从 schema_v2 抄）
3. 补 `content_vectors` 表（`embedding VECTOR(512)`，用 pgvector）

**知识点**:
- `has_vector` 标记机制（见 8.9）：向量写入成功才标 TRUE，推荐时过滤掉 FALSE 的，防止"有内容没向量"的孤儿数据。
- **建表脚本必须与代码期望的字段完全一致**。多版本 schema 并存（v2/v3）时容易漏表漏列，全新部署最容易暴露。
- 注意：`field_importance` 预置数据是从 schema_v2 抄的，`career_plans`/`news` 字段可能与 V3 新表不完全对齐，后续需按 V3 实际字段校准。

---

### 10.12 遗留的 `content_index` 数据迁移步骤（已删除）

**现象**: app 启动崩溃重启，日志：
```
UndefinedTableError: relation "content_index" does not exist
[SQL: SELECT ... FROM content_index WHERE status='active' ...]
```

**根因**: `docker-entrypoint.sh` 调用 `migrate_v3.migrate_and_fill()`，它从**旧的 `content_index` 单表**迁移数据到新分表。但这是**全新部署**，根本没有 `content_index` 表，第一步查询就抛异常 → entrypoint 失败 → 容器 `restart: unless-stopped` 不断重启。

**解决**: 该迁移是**遗留步骤**（从旧架构升级才需要），全新部署不需要。**已从 entrypoint 中删除** `migrate_and_fill()`，只保留 `insert_user_behaviors()`（导入样例用户行为）。

**知识点**: 
- 数据迁移脚本是"升级路径"而非"全新安装路径"。全新环境应跳过迁移。
- 容器 `restart: unless-stopped` 会让初始化失败的容器**无限重启**，日志会不断重复同一错误，排错时注意区分"重启前的日志"和"最新日志"。

---

### 10.13 本章经验教训汇总

| # | 教训 | 避免方法 |
|---|------|---------|
| 1 | Docker 客户端/守护进程权限分开，socket 组不对则无权限 | 正规做法：systemd 配 `SocketGroup=docker`，而非手改 socket |
| 2 | `docker compose` 和 `docker-compose` 是两种工具 | 先 `docker-compose --version` 确认服务器用哪种 |
| 3 | 内网环境 Docker Hub 连不上，公共加速器很多已失效 | 实测镜像源能否返回 tag 列表；可用源才写入 daemon.json |
| 4 | 阿里云公共仓库不是 Docker Hub 加速器 | 需要 `registry-mirrors` 专门的加速器，或阿里云账号专属地址 |
| 5 | ARM64 老内核触发 Redis 数据安全自保护 | 加 `--ignore-warnings ARM64-COW-BUG` |
| 6 | Debian 新版本的软件包名可能变化 | Dockerfile 里写发行版存在的包名，别写死版本号 |
| 7 | 大依赖包（torch）pip 下载易超时 | `--timeout` + `--retries`，或 `wget -c` 断点续传 |
| 8 | SQLAlchemy async 连接池绑定事件循环 | 多段初始化合并到同一个 `asyncio.run()` |
| 9 | 部署脚本与函数签名必须一致 | 脚本调函数前核对参数；这类错误运行时才暴露 |
| 10 | `host=/tmp` socket 只在同机有效 | 容器化后改 `PGPOOL_DSN` 网络连接，保留 socket 作本机回退 |
| 11 | 建表脚本漏表漏列，全新部署才暴露 | 核对代码所有查询的表/列与 schema 完全一致 |
| 12 | 迁移脚本是升级路径，非全新安装路径 | 全新环境跳过迁移；`restart: unless-stopped` 会让失败无限重启 |
| 13 | `docker cp` 进容器的文件会丢执行权限 | 修改容器内文件优先重建镜像，或 `chmod +x` 后 `docker cp` |
| 14 | 多 worker 各自跑初始化会互相冲突崩溃 | 无状态的 `--preload` 场景下初始化放主进程；最简单先降到 `workers=1` |
| 15 | 推理模型（reasoning model）长 prompt 响应慢/空 content | 评分任务换非推理对话模型（如 `deepseek-chat`），并放大超时 |

### 10.14 多 worker 模式下 worker 反复崩溃（首次访问正常、二次失败）

**现象**: 前端"第一次访问正常，第二次就加载失败"。后端日志显示大量：
```
INFO:     Waiting for child process [11542]
INFO:     Child process [11542] died
INFO:     Waiting for child process [11669]  (无限重复)
```
每次推荐请求 HTTP 200 但耗时 **13-14 秒**（worker 死了要重新 fork + 重新加载 BGE 模型）。

**根因**: `.env.docker` 里 `UVICORN_WORKERS=4`。本项目 `main.py` 在**每个 worker 的 lifespan 里都执行** `run_all_sync_now()`（全量同步外部接口 + 写库）+ `start_scheduler()`，并且**每个 worker 都单独加载一份 BGE 模型**。4 个 worker 同时启动时互相争抢做初始化，且初始化是**阻塞式**的，导致 worker 反复崩溃重启。

**排查过程**:
1. 前端"二次失败"，怀疑 Redis → 检查 redis 容器 healthy、`redis-cli ping` 正常 → 排除
2. 直接 curl 推荐接口两次，都 HTTP 200 但每次 14 秒 → 不正常（缓存应秒回）
3. 看日志发现 `Child process died` 大量重复 → 确认是 worker 反复崩溃
4. 确认是 `UVICORN_WORKERS=4` 多 worker + 各自初始化导致

**修复**: 把 `UVICORN_WORKERS` 改为 `1`：
```
UVICORN_WORKERS=1
```
单 worker 下不再互相冲突，worker 死亡次数从十几变为 0，首次请求稳定。

**教训**: uvicorn 多 worker（fork 模式）下，若应用启动时做**重量级、有副作用、互相冲突**的初始化（全量同步、加载大模型、启动调度器），会导致 worker 反复崩溃。简单场景先用单 worker 保证稳定。

### 10.15 LLM 推理模型（deepseek-v4-flash）响应慢/空 content 导致超时

**现象**: 单 worker 后服务稳定了，但推荐请求仍要 **13-14 秒**，且 `fallback_used=true`（LLM 降级）。日志：
```
WARNING: LLM 连接异常 (attempt 1/2): ReadTimeout
scale: Redis 缓存命中 → total=61ms (cache=hit)   ← 缓存生效，快
skill: total=13968ms (cache=miss)                ← LLM 慢
```

**根因**: 模型 `deepseek-v4-flash` 是**推理模型（reasoning model）**。它在回答前会生成大量 `reasoning_content`（思考过程）。当 prompt 较大（8 条候选内容）时：
1. 思考过程占满 token，**`content` 返回空** → 后端 JSON 解析失败 → 降级
2. 思考耗时超过 `LLM_TIMEOUT_MS=5000`（5秒）→ ReadTimeout → 每次请求要等超时 + 重试

**排查过程**:
1. 直接 curl DeepSeek `/v1/models` → HTTP 200，0.22s → 网络通
2. curl `chat/completions`（deepseek-v4-flash）→ HTTP 200，但 `content=""`、`reasoning_content` 很长、`finish_reason=length` → 确认是推理模型把 token 用在思考上了
3. 换 `deepseek-chat` 测试 → 1.2s 返回正常 JSON，`finish_reason=stop` → 确认换非推理模型可解决

**修复**:
1. `back/llm_config.json`：model 改为 `deepseek-chat`（非推理，直接输出 JSON）
2. `.env.docker`：新增 `LLM_TIMEOUT_MS=60000`（60秒，给足时间）

**效果**: 首次请求从 13.8s → **3.87s**；缓存命中从 13.8s → **0.075s**。

**教训**: 推理模型（带 reasoning_content）适合复杂推理，但**不适合"要求直接输出结构化 JSON"的评分任务**——思考会占 token 导致 content 空、且慢。应换非推理对话模型。

### 10.16 `user_profile` 表缺行为相关字段

**现象**: 同步时报 `column "behavior_tags" of relation "user_profile" does not exist`。

**根因**: `behavior_sync_v2.py` 的 `_sync_user_profile` 写入 `user_profile` 需要 `behavior_tags, preferred_content_types, last_active_at` 字段，但 schema 里 `user_profile` 表只建了 `user_id, base_tags, updated_at`。

**修复**: 补全 `user_profile` 表字段（`behavior_tags JSONB`、`preferred_content_types JSONB`、`last_active_at TIMESTAMP`）。

**教训**: 与 10.11 同理——建表脚本必须与代码实际写入/查询的字段完全一致。多版本 schema（v2/v3）演进时最容易漏字段。

### 10.17 本节补充：为什么另一台服务器 worker=32 正常，这里 worker=4 就崩？

> 详见正文回答。核心：问题不在"worker 数量"，而在"这台服务器的运行方式 + 初始化阻塞 + 单容器资源"的叠加效应。对比要点：
> - 原开发机（384 核 / 4×RTX4090 / 本机直连 PG）与这里（受限内网 / ARM64 老内核 / 容器网络 / 单 worker 加载 BGE）运行环境差异巨大。
> - 多 worker 在此环境暴露的根源是"每个 worker 阻塞式全量同步 + 各自加载大模型 + 抢占容器资源"，原开发机的高并发 + 慢初始化掩盖了这个问题。

### 10.18 6 类内容部分为空（权限过滤 + 数据写入失败）

**现象**: 前端只有课程(course)、岗位(skill)、测评(scale)有返回内容，培训班(training)、职业规划(career)、资讯(news)三个都是空的。

**第一步：核查数据库各表真实数据量**:

| 类型 | 表数据量 | 有向量 | 分析 |
|------|---------|--------|------|
| 课程 courses | 3 | 3 | 数据在 |
| 培训班 training | 2 | 2 | **数据在，但前端空** |
| 岗位 skill | 58 | 58 | 数据在 |
| 测评 scale | 76 | 76 | 数据在 |
| 职业规划 career | **0** | 0 | **真没数据（写入失败）** |
| 资讯 news | **0** | 0 | **接口未就绪，拉不到** |

**结论**: 三类空的原因各不相同：
1. **training（有数据但空）**: 数据在 `training_classes` 表里也有向量，但推荐为空。
2. **career（真 0 条）**: 数据写入数据库失败。
3. **news（真 0 条）**: 外部 `atjNews` 接口未就绪，本身无数据。

### 10.19 career 写入失败：类型不匹配 + 字段超长

**现象**: career 同步报错，`career_plans` 表一直是 0 条：
```
[career] 单条写入失败 (id=1): DataError: invalid input for query argument $2: 1 (expected str, got int)
```

**根因（第一层）**: 接口返回的 `customId`/`staffId` 是**整数**（如 `1`、`14`），而 schema 里 `customId VARCHAR(64)` 需要字符串。`content_sync_v2.py` 里 `raw.get("customId", "")` 返回整数，psycopg2/asyncpg 传整数给 VARCHAR 列报错。

**修复**:
```python
"cid": str(raw.get("customId") or ""), "sid": str(raw.get("staffId") or ""),
```

**根因（第二层）**: 修好类型后，又报：
```
StringDataRightTruncationError: value too long for type character varying(128)
```
接口返回的 `position` 字段是长文本（岗位分类说明，远超 128 字符），而 schema 里 `position VARCHAR(128)` 太短。

**修复**: 把 `position` 改为 `TEXT`。

修复后 `career_plans` 正常写入 1 条"MBTI职业性格职业规划"。

**教训**: ① 接口字段类型（int）与数据库字段（varchar）不一致时要 `str()` 转换；② 接口返回长文本的字段应设计为 `TEXT`，避免 `VARCHAR(n)` 截断报错。

### 10.20 权限表缺 training/career：主键漏 content_type（真实根因）

> **⚠️ 更正**：初版误判为"`fetch_all_apis` 并发漏类型"。经彻底排查，**真正根因是权限表主键设计缺陷**，与并发无关。并发问题为误诊，已在下文详细还原排查过程。

**现象**: career 修好后，training(2条) 和 career(1条) 数据都在库里、也有向量，但 `REQUIRE_PERMISSIONS=true` 下推荐仍为空。权限表 `user_content_visible` 只有 course(3)、skill(55)、scale(53)，**没有 training 和 career**。

**排查过程（逐步还原）**:
1. 权限表无 training/career → `get_all_candidates` 无权限记录返回空。
2. 逐个调用 5 个接口都能返回数据 → 接口正常。
3. **第一次误判**：怀疑 `fetch_all_apis` 用 `asyncio.gather` 并发调 5 接口，受限网络下部分超时被 `_safe_fetch` 吞掉 → 改串行。
4. **串行后问题依旧**：training/career 仍不进权限表 → 排除并发因素，**并发是误判**。
5. **关键矛盾**：日志显示 `shty80001: 权限同步完成, 140 条可见内容`（`inserted=140`），但权限表实际只有 111 条（course3+skill55+scale53=111，缺 29 条 + training2 + career1）。
6. 对比各内容表 `origin_id`：course=1,2,3；training=1,2；career=1；skill=1..58。**所有类型的 origin_id 都是独立编号，都从 1 开始**。
7. **真相**：`user_content_visible` 主键是 `PRIMARY KEY (user_id, origin_id)`，**缺少 `content_type`**。于是 course 先插入占掉 (user,1)、(user,2)、(user,3)；training 的 origin_id=1、2 和 career 的 origin_id=1 与已有记录**冲突**，被 `ON CONFLICT DO NOTHING` **静默跳过**。skill/scale 也因 id 与 course 前几个重复被部分跳过。

**根因**: 权限表主键 `(user_id, origin_id)` **没有包含 `content_type`**。不同内容类型的 `origin_id` 独立编号（都从 1 开始），跨类型必然冲突，被 `ON CONFLICT (user_id, origin_id) DO NOTHING` 静默丢弃，且**无任何报错**，极难察觉。

**为什么"不同的内容表"会 id 冲突？**（通俗解释）
- 各内容表（courses/training/skill/career）**彼此独立**，每张表自己的 id 都从 1 开始，这**没问题**——不同表之间本来就不冲突。
- 但权限表 `user_content_visible` 是**所有类型的"汇总表"**，它把 5 张内容表的 origin_id 全部塞进**同一张表**。此时主键 `(user_id, origin_id)` **只靠 `user_id + origin_id` 两个值**判断是否同一条，**没有 `content_type` 这个区分维度**。
- 于是按插入顺序（course→skill→scale→training→career）：course 先插入 (user,1)(user,2)(user,3)；之后 skill 的 (user,1) 与 course 的 (user,1) **主键值相同 → 被 `ON CONFLICT DO NOTHING` 当成"已存在"丢弃**；training/career 同理全被丢弃。
- 类比：三个班的同学都从 1 号开始编号，把三个班花名册**合并成一本**，若只按"学号"登记，3 班的 1 号会和 1 班的 1 号冲突。必须用"班级+学号"（`(content_type, origin_id)`）才能区分。修复主键加 `content_type` 就是这个道理。

**修复（两处，必须一致）**:
1. `schema_v3.py`：主键改为三字段
   ```sql
   PRIMARY KEY (user_id, content_type, origin_id)
   ```
2. `api/recommend.py`：`_sync_user_permissions` 的 `ON CONFLICT` 同步改为三字段
   ```sql
   ON CONFLICT (user_id, content_type, origin_id) DO NOTHING
   ```
   （否则报 `there is no unique or exclusion constraint matching the ON CONFLICT specification`）

**修复后效果**（`REQUIRE_PERMISSIONS=true` 生产模式下）:
```
权限表: course=3, training=2, skill=58, scale=76, career=1  (全部完整)
推荐: skill:4条, scale:4条, course:3条, career:1条, training:1条  ✅
```

**教训**: `ON CONFLICT DO NOTHING` 配合不完整的主键，会**静默丢弃**冲突数据且不报错。排查"某类数据莫名缺失"时，若日志显示"已插入 N 条"但库里对不上，应重点怀疑**主键/唯一约束与去重逻辑不一致**（尤其当不同类型使用独立编号、id 都从 1 开始的情况）。

### 10.21 本节小结

| # | 教训 | 避免方法 |
|---|------|---------|
| 1 | 前端某类内容为空，先查数据库该表数据量 + 有向量数 | 区分"真没数据" vs "有数据但被过滤" |
| 2 | 接口 int 字段写入 varchar 列报 DataError | 写入前 `str()` 转换 |
| 3 | 长文本用 `VARCHAR(n)` 会超长截断报错 | 长文本字段用 `TEXT` |
| 4 | `ON CONFLICT DO NOTHING` 配不完整主键会静默丢弃数据 | 主键/唯一约束必须与去重字段完全一致（详见 10.20）|
| 5 | 权限过滤会"静默"过滤整类内容，不易察觉 | 先查数据量区分"真没数据" vs "被过滤" |

### 10.22 BGE 编码性能排查与优化（并发瓶颈）

**背景**: 压测发现 QPS 只有 18，疑似 BGE 编码是瓶颈。实测验证如下。

**实测数据**:
| 场景 | BGE 初筛耗时 |
|------|-------------|
| 单请求 | 50ms（占单请求 1.5-2%）|
| 20 并发 | 73~290ms（3-6 倍退化）|

**关键结论（纠正误判）**:
1. BGE 编码单请求只 50ms，**不是单请求瓶颈**；但**并发时退化到 290ms**，成为并发吞吐瓶颈。
2. 初筛耗时 = `build_user_vector_text`(每次) + `encode_text_async`(BGE编码,每次) + pgvector 检索。

**问题 1：用户向量重复编码（浪费 6 倍）**
- `run_recommend_v2` 遍历 6 个类型，每个类型都调 `_recommend_for_type` → `pre_screen_candidates` → `vector_search` → `encode_text_async`。
- 用户向量 = `build_user_vector_text(profile, behaviors)` 的固定输出，**同一请求内完全相同**，却算了 6 遍。
- **修复**：`run_recommend_v2` 开头编码一次用户向量，作为 `user_vector` 参数传给所有类型复用。`vector_search`/`pre_screen_candidates` 增加可选参数 `user_vector`，传入则不再重新编码。
- **效果**：初筛耗时从 50ms → **2ms**，QPS 从 18 → 55（提升 3 倍）。

**问题 2：torch 内部 OpenMP 线程过载**
- 默认 `torch.get_num_threads()` = CPU 核数 = **96**。单次编码用 96 线程，对 BGE-small 小模型**调度开销 > 并行收益**。
- 高并发时：多个请求 × 96 线程 = 线程爆炸，严重争抢 CPU 核 → 并发编码变慢。
- **修复**：`_load_model()` 里 `torch.set_num_threads(int(os.environ.get("BGE_NUM_THREADS", "1")))`，单次编码用 1 线程，并发靠多 worker（进程）并行。

**知识点：GIL / 线程 / 进程 / 共享模型**
- **GIL**：CPython 的全局解释器锁，同一时刻只有一个线程执行 Python 字节码。**但 torch 的 C 层矩阵运算会释放 GIL**，多个 OpenMP 线程能真正并行。
- **多线程同模型**：模型权重共享（只读，无数据竞争），但推理的临时张量/输入输出各线程私有；受 asyncio 线程池大小 + Python 层 GIL + OpenMP 争抢限制，并发编码会被串行/排队。
- **多进程（worker）**：内存独立（各一份模型），进程间真正并行（无 GIL），是 CPU 密集任务的最佳并行方式。

**为什么并发会"串行化/排队"（三个叠加原因）**:
1. `asyncio.to_thread` 用默认线程池（默认 `max_workers=min(32, cpu+4)`），**线程数有限**，请求多时排队。
2. `SentenceTransformer.encode()` 的 **Python 层**（token化、后处理）受 GIL 限制，线程间串行。
3. torch **OpenMP 线程争抢 CPU 核**（96 线程），多个编码互相拖慢。
> 模型只读 + C 层无 GIL 是对的，但"共享线程池有限 + Python 层 GIL + OpenMP 争抢"才是并发编码变慢的真因。

**教训**: ① 高并发小模型推理，**单次编码用单线程(`set_num_threads(1)`) + 多进程(worker)并行**最优；② 同一请求内固定的向量（用户向量）应**只编码一次复用**；③ 排障先实测各环节耗时占比，别凭直觉断定瓶颈。

### 10.23 专题：模型推理时多线程之间的真实关系

> 本节系统讲清楚"多线程/多进程使用同一个模型"时，内部到底怎么运作。回答的核心问题：
> ① `set_num_threads(96)` 是同时用 96 条线程吗？② 每条线程算的内容一样吗？③ 都调用同一个模型吗？④ GIL 下是重复执行 96 次吗？

**基础概念：进程 / 线程 / GIL**
| | 进程 | 线程 |
|---|------|------|
| 内存 | 独立（各一份模型）| 共享（同一模型对象）|
| GIL | 无限制（进程间真并行）| 受 GIL 限制（Python 层串行）|
| 适合 | CPU 密集 | I/O 密集 |
> GIL = 全局解释器锁，同一时刻只有一个线程执行 **Python 字节码**。

**Q1：`set_num_threads(96)` 是同时用 96 条线程吗？—— 是"调度"，不是"重复"**
- `torch.set_num_threads(96)` 设置的是：每次矩阵运算时，内部 **OpenMP 线程池最多用 96 个线程并行**。
- 一次 `model.encode()` 内部有很多矩阵乘法，例如 `[1×512输入] × [512×768权重] = [1×768输出]`。
- OpenMP 把这 96 个线程**分工**去算这个矩阵的不同分块，**共同完成一次运算**，不是各算一遍。

**Q2：每条线程算的内容一样吗？—— 不一样，是"分工"**
- 线程 0 算结果矩阵的第 1 块，线程 1 算第 2 块……线程 95 算第 96 块。
- 各线程计算结果**不同部分**，拼起来才是完整结果。这就是**并行加速**（96 线程理论上快 96 倍）。

**Q3：这 96 条线程调用同一个模型吗？—— 是，共享同一份只读权重**
- 模型权重（矩阵）是**共享只读**的，96 个线程只读它，各自取自己负责的部分。
- 因为**只读不修改**，多线程同时读同一块内存是安全的，**无数据竞争、结果正确**。

**Q4：GIL 限制下，是不是重复执行 96 次？—— 不是，且关键在"GIL 对 C 层无效"**
- **GIL 只锁 Python 字节码**。torch 的矩阵运算（C/C++/OpenMP 实现）**执行到 C 层时会释放 GIL**。
- 所以 96 个 OpenMP 线程能**真正并行**跑在 96 个 CPU 核上（不受 GIL 限制），是"真正并行计算 1 次"，不是重复 96 次。

**为什么高并发下反而变慢？—— 不是重复计算，而是"线程过载 + 争抢"**
1. **单次编码用 96 线程对小模型是浪费**：BGE-small 一次矩阵运算很快（几 ms），但创建/调度 96 个线程的**开销 > 并行收益**。
2. **并发时线程爆炸**：20 个并发请求 × 96 线程 = 1920 个线程抢 96 个 CPU 核 → **疯狂上下文切换**，每个都变慢。
3. 所以 `set_num_threads(1)`：单次编码 1 线程（稳定），并发靠多进程（worker）真正并行。

**多线程共用同一模型时，线程怎么维护自己的数据？（数据拷贝）**
| 数据 | 归属 | 是否拷贝 |
|------|------|---------|
| 模型权重 | **共享**（只读）| 不拷贝 |
| 输入文本/张量 | 各线程私有 | 构造时拷贝 |
| 中间激活张量 | 各线程私有 | 每层计算产生（临时）|
| 输出向量 | 各线程私有 | 结果拷贝回 Python |
- 各线程的输入/中间/输出**私有**，所以无数据竞争、结果正确。
- **拷贝有真实时间开销**，但对 BGE-small（512 维）占比小，主要耗时在矩阵运算。

**每个线程创建自己的模型副本，耗时吗？—— 非常耗时**
- `SentenceTransformer(MODEL_PATH)` 加载：读磁盘几百 MB 权重 + 构建网络 + 预热，**实测 10-20 秒**。
- 每线程/每进程创建副本 = 每次加载 10-20 秒 + N 倍内存。所以**不推荐**每线程/每进程都加载副本。

**为什么并发编码还是会"串行化/排队"？（三个叠加原因）**
1. **`asyncio.to_thread` 线程池有限**：默认 `max_workers=min(32, cpu+4)`，请求多时排队。
2. **`encode()` 的 Python 层受 GIL**：token化、后处理等 Python 代码线程间串行。
3. **torch OpenMP 线程争抢 CPU**：多个编码都想用 96 线程，互相抢核变慢。
> 模型只读 + C 层无 GIL 没错，但"共享线程池有限 + Python 层 GIL + OpenMP 争抢"才是真因。

**本专题结论**：对"高并发小模型推理"，
- 单次编码：`set_num_threads(1)`（避免 OpenMP 争抢）。
- 并发：靠**多进程 worker**（真正并行，无 GIL）。
- 内存：每 worker 一份模型是合理的（进程级并行），服务器内存充足时无需强行共享。

**实测压测效果（缓存命中场景，详细见 9.6）**：
| worker 数 | 并发20 QPS | 并发50 QPS | 失败 |
|-----------|-----------|-----------|------|
| 1 | 18.3 | - | 0 |
| 4 | 55.8 | - | 0 |
| 8 | 93.8 | - | 0 |
| 12 | 93.8 | 139.0 | 0 |
| 16 | - | 142.3 | 1 |
> 最终定为 **8 worker**（最优性价比）。QPS 极限约 140，瓶颈在并发度 + 共享资源（DB/Redis 连接池），非 CPU。

### 10.24 空内容候选被 LLM 幻觉打分（prompt 约束修复）

**现象**: 课程(course)接口返回的数据全为空（`courses` 表 `name`/`texts1-4` 全空，`raw_data={}`），但推荐时 LLM 却给这些空课程打了 0.6+ 的高分，并生成看似"个性化"的理由（如"计算机专业高度匹配""就业搜索相关性强"）。

**根因分析（三层）**:
1. **BGE 初筛未筛掉空内容**：`pre_screen_candidates` 里有 `if len(all_candidates) <= max_keep: return all_candidates`（候选 ≤30 时全部接受）。课程只有 3 条，**全部进入**，没被语义筛掉。
2. **LLM 幻觉**：原 prompt 只写"根据用户专业/技能/行为与候选内容的匹配度打分"，**没有强制要求基于候选内容本身**。当候选内容为空时，LLM 只能根据**用户背景**臆造打分和理由 → 产生"计算机专业高度匹配"这类看似合理、实际无依据的幻觉。
3. **保底逻辑**：即使分数低，也会保底返回最高分 1 条，保证类型有结果。

**关键区分（面试可讲）**:
- **模板理由**：`_template_reason` 写死，固定文案"根据您的背景为您推荐该课程"。
- **LLM 降级理由**（空课程）：LLM 实时生成，但**只基于用户画像/行为**（没看到课程内容），所以"计算机专业高度匹配"——看似个性化，实则是基于用户侧的无依据猜测。
- **LLM 真实理由**（有内容 skill）：基于用户画像 + **候选实际内容**，如"专业学历匹配，岗位高度对口"——最可信。

**修复**: 优化 `llm_scorer.py` 的 prompt，新增【评分铁律】：
```
1. 打分必须严格基于候选的实际内容（名称/字段文本），结合用户背景评估匹配度；
   不得仅凭用户背景臆断"该内容适合用户"。
2. 若候选内容缺失（名称或关键字段为空/极短），应给低分（score ≤ 0.3），
   reason 明确写"内容信息缺失，暂无法评估"，严禁虚构具体匹配理由。
3. 只对有真实内容、且与用户背景确实相关的候选给高分。
```

**修复后效果**:
| 类型 | 优化前 | 优化后 |
|------|--------|--------|
| 空课程 course | score 0.6+，理由"计算机专业高度匹配"（幻觉）| score **0.27**，理由"内容信息缺失，暂无法评估" |
| 有内容 skill | 0.89 + 真实理由 | 0.89 + 真实理由（**不受影响**）|

**教训**:
- **LLM 打分必须约束"基于候选内容本身"**，否则空/脏数据会被 LLM 凭用户背景脑补出高分和合理理由（幻觉）。
- **prompt 要显式处理"数据缺失"**：明确"空内容打低分 + 诚实理由"，避免虚构。
- 排查"理由为何失真"时，要区分**模板 / LLM 基于用户 / LLM 基于内容**三种来源。
